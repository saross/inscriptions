# Draw-wise H3b outputs are the finalised base-run deliverables (Decisions 1–3
# resolved 2026-06-15) and ARE tracked — JSON/CSV/figures regenerate from the
# committed code + the cc-refit posterior draws via code/run_h3b_drawwise.py.
code/__pycache__/
