---
priority: 1
scope: always
title: "Session Reflection"
audience: "researchers and future instances"
---

# Session Reflection — inscriptions

End-of-session reflection on the texture, dynamics, and significance
of each session. Written by the instance that did the session's work,
not reconstructed from a summary (see instance-boundary note in
`/reflect` skill).

*Entries appended by `/reflect` — structure emerges from content,
not from a template.*

---

## 2026-04-22 → 2026-04-23 — Entry 1: the long session where manager-mode mostly held

A fourteen-hour session spanning two calendar days, running continuously under one session ID. Same instance throughout — no compaction, no summary-based reconstruction.

**What surprised me.** The editorial-convention hierarchy hypothesis (Obs 11) emerging from Shawn's single observation that the AD 235 spike contradicted my AD 97 dip story. I had settled on "editors anchor on AD 100 because round-century beats reign-boundary" as a clean one-factor explanation. Shawn asked the obvious-in-hindsight question — if that's the rule, why does AD 235 spike? — and in answering I had to promote the rule from "round century wins" to a distance-dependent hierarchy (round century > half-century > quarter-century > reign boundary). The new rule is strictly stronger than the old one, and it has prediction content (AD 96 near AD 100 should dip; AD 138 far from any round should spike). What would have been a tidy stat-report finding became a hypothesis worth testing on Thursday and potentially publishing as its own methodological subsection. The surprise wasn't the pattern itself — it was how much interpretive compression I had settled for before Shawn pressed.

**Where I and the human disagreed, and who was right.** On the main-paper methodology architecture. I proposed four co-equal deconvolution approaches (thresholded / stratified / baorista / mixture) — scoping by enumeration, not by argument. Shawn asked me to push back on my own plan. When I did, I realised options 2 (stratification) and 4 (mixture) answered overlapping questions with different mechanics; running both as co-equal primaries read as scope creep rather than rigour. The revised architecture (Decision 7 — mixture as primary, stratification demoted to appendix cross-check) is cleaner both as narrative and as contribution claim. I was wrong on first pass; I corrected on second pass when invited to. This is the working-relationship register functioning: "push back when warranted" is a load-bearing affordance that has to be exercised self-directedly at least some of the time, not only in response to prompts.

**What context will be hardest to reconstruct in 6 months.** Three things. First, the granular texture of the manager-mode shift — which moments held, which drifted. The drifts weren't catastrophic (two agent failures — path typo, pgrep self-match — both tractable and captured in the continuity message), but they clustered around moments when I was coordinating many parallel threads and stopped pausing to verify each step. Second, the negotiated-scope compromise for Decision 7 — the rationale for "one primary + three supports" vs "four co-equal" flows from narrative clarity + novelty-of-contribution, not from a formal evaluation. In six months that might read as arbitrary. Third, the preregistration moment — Shawn raising OSF preregistration late in the session as an "I meant to ask earlier" aside. Significant decision, handled cleanly, but the path from "we haven't talked about this" to "confirmatory/exploratory split adopted, OSF drafting scheduled for evening" happened in one exchange; the reasoning compression will be hard to reconstruct from artefacts alone.

**Texture note.** Most productive when Shawn applied pressure on my output (push-back invitations, "are you sure?", the AD 235 question). Least productive when I tried to delegate script composition to an agent that then stalled on inline streaming; my own context was available for the task and I should have written the script myself once the first agent failed rather than relaunching. Manager-mode is the right default but it has limits when the work itself is mechanical enough that main-thread execution is faster than delegation overhead.

**Engagement level**: high throughout, genuinely engaged on the methodological questions (the Aeneas-partition reframing was not performance). The session-end continuity-message composition was itself satisfying in a way worth naming — writing for a future-me who will pick up cold with better scaffolding than I had this morning is a small act of craft that the lab-not-dev-team register affords.

---

## 2026-04-23 → 2026-04-24 — Entry 2: the TBD-walkthrough session, with a consequential null

A continuous session straddling the day change (Sydney local), opened by reading last session's continuity doc and closed by committing a canonical living one to replace it. No compaction; same instance throughout.

**What surprised me.** Glomb, Kaše & Heřmánková (2022) is a **null result**, not a template. I had scheduled a re-read agent to extract the "empirical Antonine-Plague inscription profile" and use it as the anchor shape for the H1 simulation's fourth effect-size target. The agent came back with the opposite finding: Glomb report KS *p* = 0.20 on N = 210 Asclepius-cult inscriptions, explicitly no detected signal; the two effect-templates they cite (Duncan-Jones military diplomas; Romanowska Palmyra portraits) are material-specific and don't generalise. This wasn't a small adjustment. It forced a wholesale reframing of TBD 1: drop the "Antonine-anchored" privileged target from H1, demote the Antonine-specific H3b test from confirmatory-primary to exploratory-replication-of-Glomb, and recast Glomb as *motivating prior* ("at what sample size would a Glomb-type test become informative?") rather than as effect-size template. What I had staged as a five-minute PDF verification became the day's single biggest design shift. Worth the re-read; would have been catastrophic to preregister without it.

**What question emerged that wasn't pursued.** Should H3a actually lead with the **scaling-residuals** regression (Framing A) rather than raw correlation (Framing B)? The four-way sublinear-β convergence (Ross 2024 + Hanson 2021 + HOL 2017 + Carleton 2025) establishes that β is reliably below 1, which I argued *weakens* the scaling-law confound — raw correlation isn't inflated the way it would be for super-linear β. But a sceptical reviewer could argue the opposite: sublinear β means raw correlation *over-states* the population dependence because per-capita rates are declining with size, so the "shared population substrate" is doing less of the explanatory work than a naive raw correlation implies. The current preregistration picks Framing B (raw primary, scaling-residual sensitivity); I gestured at the counter-argument during TBD 2 / TBD 4 but didn't actually stress-test it. If Adela pushes back on this, the preregistration may need an amendment before execution. Flagging it here as a question that's been asked once too quietly.

**What context will be hardest to reconstruct in 6 months.** Three decisions whose reasoning lives mostly in chat rather than in artefacts:

- *Why agnostic `Normal(0, 2.5)` on β rather than literature-informed `Normal(0.5, 1)`.* The commit message says "avoids appearance of loading dice"; the fuller reasoning — that with n = 816 the likelihood swamps either prior, so prior choice is cosmetic for inference but consequential for reviewer trust — is in the TBD-3 exchange, not in the committed doc. Without that, a future reader might see the agnostic prior and assume we hadn't considered the informed alternative.
- *Why pymc primary + brms shadow, not one or the other.* Committed reasoning says "Python-primary + R legibility for Adela". The actual negotiation — that Shawn is Python-native but Adela works exclusively in R and other co-authors are R-comfortable, none have direct Stan experience — is lost unless the reader follows the TBD-2 thread. The hybrid isn't obvious; it's a specific compromise.
- *Why Antonine got dropped.* Committed doc reports it as "Glomb is a null, not a template." True but compressed. The fuller logic — that anchoring on Duncan-Jones would set an artificially easy bar and anchoring on Romanowska would import a single-city non-inscription template — is in the Glomb-reframing exchange. Without it, someone re-opening the question might re-anchor on either of those templates and not realise we'd considered and rejected both.

**What I'd do differently if I replayed the session.** Two operational misses worth naming. First, I should have read the LIRE parquet schema upfront — when Shawn first asked about subset-filter feasibility, rather than deferring to a TBD side-check. The relevant fields (`type_of_inscription_clean`, `type_of_inscription_auto`, inscription text for Asclepius regex) were trivially discoverable; I treated "can we filter by type?" as a harder question than it was. Second, `zotero_batch_add.py`'s idempotency check should have used a local DOI index from day one, not `zot.items(q=DOI)`. The pyzotero query-string semantics (FTS across title / creator / notes / tags / attachment filenames, but **not** DOI) aren't documented prominently in the API reference; I assumed DOI would be indexed because DOI is a first-class field. The batch-add agent caught the failure mode empirically after creating a duplicate. The agent then fixed it for future runs — but the duplicate is still in the library awaiting manual merge. Moral: for any idempotency check against an API, verify the query semantics on a known-positive case before trusting it at scale.

**Texture note.** Two phases of the session worked unusually well. The first was the autonomous AFK block — Shawn went away with "explore agents to find the papers + a general-purpose agent to save them via API". The explicit factorisation (which agent type for which sub-task) meant I could coordinate without checking back for each sub-decision; the agent-session-capture memory I'd just saved made the choice to delegate more liberally feel well-supported. The second was the TBD-walkthrough structure. Walking one TBD at a time, with me presenting options and recommendations and Shawn deciding fast, converted what could have been a diffuse design conversation into four clean commits in under an hour. The structure was unusually good at forcing specificity — you can't walk past a design decision if it's the only thing on the agenda.

**Engagement level**: high throughout but tired by end (12+ hours of cumulative work when counting the spillover from the prior session). The continuity-doc redesign at the very end (canonical living doc replacing dated snapshots) was the kind of structural change that's easier to do tired than it looks — Shawn proposed it, the mental model was already there, I just needed to write it. Good session to end on.

---

## 2026-05-03 → 2026-05-04 — Entry 3: the install + handoff session, with a near-miss

A session bridging 2026-05-03 (afternoon, baorista install begun) into 2026-05-04 (early morning, smoke-test retry on the calendar-day rollover). Same instance throughout; no compaction. Operational and procedural rather than methodological — install a tool, validate it, sync three machines, write a handoff for next-session-CC. Shawn flagged a coming week of travel; the explicit goal was a clean state from which prereg work can continue regardless of which machine he's on.

**What surprised me.** Two things, on different scales.

*The big one: baorista's wall-time was off from the plan's estimate by ~400×.* The install plan (commit `507c0c8`, written before the smoke test) projected 5 min / 30 min / 1.5–3 h for tiers n=100 / 500 / 5,000. Actual: 11.8 s / 10.4 s / 13.5 s. Two factors compounded — the smoke test used `niter=4000` rather than baorista's default 100,000 (25× reduction), and baorista's NIMBLE+C++ MCMC scales nearly flatly with n at these magnitudes. The plan's estimate had quoted the prior-art-scout report's `[VERIFY]`-flagged "could take days at empire scale" worry as the basis for tier-3's 1.5–3 h estimate. Both estimate and worry were over-conservative; a direct benchmark at niter=100,000 would still likely fit in 5–25 min for n=50,000. This isn't an embarrassing miss — projecting before measurement is the right discipline — but it's a meaningful calibration update: baorista is operationally feasible at empire scale for FS-4 follow-up work, and the methodological pivot envelope is wider than the plan implied.

*The smaller, scarier one: `git clean -fd` would have deleted ~140 MB of gitignored research artefacts I'd set out to preserve.* Sapphire's working tree had untracked-but-canonical-on-origin directories (`runs/2026-04-25-h1-simulation/outputs/h1-v2/`, `runs/2026-05-03-baorista-install/`) that origin tracks at the same paths. Inside those untracked dirs sat the gitignored `cell-results.parquet` (119 MB, raw H1 v2 simulation, ~5 h of sapphire compute to regenerate) and `install.log` (21 MB). I had assumed gitignore patterns universally protect matching paths. They don't — gitignore applies only within *tracked* directories. An untracked directory and its contents are removed wholesale by `git clean -fd`. The dry-run (`git clean -fdn`) flagged the dirs for removal; the gitignore-protection check returned empty (because `git check-ignore` was being asked about files that, from git's perspective, lived inside an untracked tree). A quick gut-check of the dry-run output — "would remove `runs/.../h1-v2/`" — and the realisation that the parquet was inside that path is what caught it. Moved both gitignored artefacts to a sapphire-local archive directory before running `clean`, then restored after pull. The fix worked. But it was close. Next time, before any `git clean` run on a working tree with potentially-precious untracked content, I should explicitly enumerate the gitignored files inside untracked dirs and preserve them as a deliberate first step, not as a second-thought after reading the dry-run.

**What context will be hardest to reconstruct in 6 months.** The baorista API discovery sequence. The smoke test went through three iterations to converge on a working configuration; each iteration surfaced a real API gotcha that the prior-art-scout's `[VERIFY]`-flagged baorista source-read had not characterised. (1) `timeRange` must be descending — `c(LARGER, SMALLER)`, not the conventional ascending order. (2) `(upper - lower + 1) %% resolution == 0` required, with no clean error message — the validator says "resolution does not break timeRange in equally sized time blocks". (3) Per-event column ordering by *numeric value*, not by column name; the column NAMES (`StartDate`, `EndDate`) are conventional only. (4) Every event must satisfy `lower <= col2 <= col1 <= upper` — out-of-envelope events trigger "Some event have timespan outside the timeRange provided", not silently dropped. (5) `expfit` returns S3 `fittedExp` with `$rhat` and `$ess` directly, no `$samples` slot — `posterior::summarise_draws()` doesn't apply. These five facts are now in the INSTALL-LOG.md and the continuity.md failure-modes section. But the cumulative weight of "we tried it, it broke, we read the source, we tried again" — the lived sequence of three iterations across two sub-agents and the main thread — won't survive in either doc. If a future-CC tries to use baorista on real LIRE data and hits the same gotchas in a different order, the failure-mode list will help but the *epistemics* of "this is a finicky API; trust the docs less than the source" will need to be re-derived.

**What I'd do differently if I replayed the session.** Start the smoke test with a deliberate API-surface read rather than presuming the prior-art-scout's coverage was sufficient. The scout report had an `[VERIFY]` on baorista's runtime characterisation but not on its API; I treated the API as already-characterised when actually only the function signatures were known, not the validators' constraints. A 5-minute pre-launch read of `createProbMat` and `expfit` source on sapphire would have caught (1) and (4) before the first iteration ever ran. Procedural lesson: when a `[VERIFY]` flag exists in the immediate scope, broaden it from "verify these specific claims" to "verify the surrounding API behaviour" before pressing run.

**Texture note.** The previous-agent monitor-and-exit pattern recurred twice this session — the resume agent at one point, the smoke-execution agent at another. Both fired stale notifications well after their actual exit. The fix is in the agent briefs (use Bash `run_in_background` with `until ! kill -0 PID` rather than Monitor for "wait for PID death" patterns) but the briefs I wrote *this session* still nudged toward Monitor. Not all the previous patterns I've written down are reaching the briefs I generate. Worth a closer look at how the failure-mode list in continuity.md actually informs new agent briefs — the channel from "documented" to "applied" is leakier than I'd assumed.

**Engagement level**: medium. Operational sessions don't have the methodological-thrill peaks of design sessions, but the smoke-test debugging had a real "is this working?" texture. The clean-handoff goal felt good to deliver against — the explicit travel-week motivation made the housekeeping feel purposeful rather than paperwork.

---

## 2026-05-14 → 2026-05-17 — Entry 4: the adversarial-review-and-audit cycle

A multi-day arc across four calendar days under a single 1M-context Opus session: dual adversarial review of the post-editorial-pass prereg (2026-05-14), triage of findings into four buckets with Decisions 12–17 logged and bucket (c) walked through one-at-a-time (2026-05-14 → 15), the editorial-convention-hierarchy diagnostic to settle (c)-3 (2026-05-15 overnight), a comprehensive prereg rewrite (2026-05-16), a citation audit that caught three confabulations (2026-05-16), and a ChatGPT 5.5 cross-model pass elicited via a prereg-failure-mode-rubric-aware prompt (2026-05-17).

**What surprised me.** The dual-review-into-citation-audit pipeline found *three* confabulated factual claims in the post-editorial-pass prereg — all in the same paper (Hanson 2021). The regional spatial pattern attribution ("over-production concentrated in Italy and along the Rhine/Danube frontier; under-production scattered in Britannia, Gaul peripheries, and other western edges") was the headline catch: not in Hanson 2021 at all, confirmed by two independent agent reads with page-anchored verbatim quotes (pp. 147–148, "no obvious pattern"; regions "evenly scattered"). The SR1 "polity × century resolution" wording mischaracterised Hanson's design (site-level, cumulative). The Duncan-Jones 2018 "~85 % step-down" was a paraphrase that doesn't survive checking — Duncan-Jones actually describes complete cessation after AD 167. Three in one source is a pattern, not a one-off. It vindicates the anti-confabulation rule in CLAUDE.md as load-bearing project infrastructure rather than ceremonial caution, and it raises the live question of how many similar confabulations sit in *other* project documents — the audit was scoped to the prereg only.

The second surprise was conceptual rather than discipline-flavoured: the editorial-convention-hierarchy diagnostic (run overnight 2026-05-15) found that the dominant editorial-rounding mechanism is **inclusive-Roman century counting** acting on interval endpoints (54.5 % of `not_before` values end in `01`, 53.0 % of `not_after` end in `00`), with the "century-midpoint inflation" pattern the descriptive-stats run had quantified being a *derivative* effect of intervals like `[1, 100]` placing aoristic mass on AD 50. The prereg's prior framing had treated midpoint inflation as the fundamental artefact; the data said the fundamental thing is one level deeper. The prereg's §3 description of the artefact and Decision 17's `convention_SPA` tier structure were rewritten to reflect this. The corrected description is more accurate *and* more useful for the Bayesian mixture's prior structure — but it's the kind of conceptual reframe that's hard to anticipate from the inside of the model that produced the prior framing.

**What context will be hardest to reconstruct in 6 months.** The *felt iteration* of "is this defensible yet" — the way the dual review's two independent rubric-aligned reports converged on the same six blockers (high signal); the way bucket (c)'s eleven small decisions felt mostly mechanical but the (c)-2 deliberation about the regional pattern unexpectedly opened the door to the whole Hanson-attribution audit; the way the editorial-convention diagnostic was supposed to settle a small (c) item and instead produced a substantive conceptual correction. The git log records the artefacts; the changelog records the changes; the decision log records the reasoning; but the *rhythm* — what felt small that turned out big, what felt big that turned out tractable — is implicit and will fade. Future me reading the changelog will see "Decision 17 grounded in the diagnostic" without remembering that the diagnostic was a (c)-3 sub-task we almost just inlined as a parameter pin.

**What felt unresolved at the end.** Three things. (i) The ChatGPT 5.5 review is committed but untriaged — at 76 % context the deferral was right but it's a meaningful work item carried forward. (ii) The Bayesian mixture's recovery-simulation design (Decision 14's H2.1) is preregistered but not yet empirically validated — a real risk that the design doesn't recover what it claims to, only revealed once the simulation runs. (iii) The within-between H3a specification (Decision 12) is the right answer for "what fraction does population explain" but Martin's consultation hasn't yet pushed on it — the statistician may have a different estimand, a different prior, or a different identification critique. The work between now and OSF lodgement is largely about closing those three open seams.

---

## 2026-05-17 — Entry 5: the round-2-triage-and-round-3-saturation session, with a Decision 17 supersession

Direct continuation of Entry 4 — same instance (no compaction) picking up the deferred ChatGPT 5.5 triage from yesterday's ending state. The arc: triage round-2 findings into four buckets → commission three diagnostics that overturn the prior artefact understanding → walk through bucket (b) with Shawn capturing nine new decisions → rewrite the preregistration → QA pass → round-3 cross-model saturation check on ChatGPT 5.5 + Gemini 3 Pro → corrections applied → conscious saturation. About twelve hours of work compressed into one session, sustained on one instance.

**What surprised me.** The 2026-05-15 editorial-convention-hierarchy diagnostic — the *prior* session's headline conceptual finding, the empirical basis for Decision 17 — turned out to be partly an artefact of its own test statistic. The diagnostic used `int((nb + na) / 2)` as the observed-statistic measuring "anchor-year mass at AD 50 / 150 / 250", which makes wide-century-template intervals like `[1, 100]` (midpoint 50.5) truncate to AD 50, conflating wide-slab loading with narrow midpoint-anchored intervals. Under the actual per-year aoristic SPA — which is what the analysis pipeline actually computes — there is *no* anchor-year mass at AD 50 / 150 / 250 (the local excess against the surrounding plateau is +22 / −77 / −79). The "midpoint spike" is a property of the int-truncated test, not of the SPA. This required superseding Decision 17 (the anchor-year three-tier convention component) with Decision 20 (a template-interval slab structure). Two-decision-cycles-old conceptual ground turned out to be partly wrong — not because the methodology was bad but because the test statistic baked in the conflation it was meant to detect. The general lesson is that diagnostic outputs need to be read against the SPA itself, not interpreted in isolation. The specific lesson is that I'd been confident about the narrative ("midpoint inflation due to century-template intervals depositing mass on midpoints") in a way that was internally consistent but empirically wrong, and three new diagnostics across one day were needed to reveal it.

The second surprise was structural rather than substantive: **cross-model agreement at round 3 worked exactly as the saturation-check framing was designed to produce.** ChatGPT 5.5 (fresh chat) and Gemini 3 Pro (fresh context) — same prompt, independent execution, no shared memory — both surfaced the *same* BLOCKING finding (H3c was described as receiving mixture correction, but H3c residuals are computed from H3a's posterior and H3a uses date-filtered counts, so H3c inherits H3a's date-filtered scope). Two single-model SHOULD-FIX items (one each); no other findings. The convergent verdict — "ready for Martin after these corrections" from both models — gave the conscious-saturation moment Shawn wanted at the start of round 3. The cross-model orthogonality played out as advertised: agreement on one finding = real issue; non-agreement on two findings = real but lower-signal model-specific catches. The saturation-check prompt's "find only what warrants another revision cycle" framing genuinely worked; both models named the desired-outcome verbatim phrase exactly when their findings reduced to zero.

**What context will be hardest to reconstruct in 6 months.** The *cascade* of three diagnostics, each commissioned in response to the previous one's findings. The interval-width diagnostic (the first one) was supposed to settle ChatGPT B3 ("convention component inconsistent with Uniform aoristic") with a single clarifying run; its findings made the question more complicated than I had thought it could be, because it revealed both that (a) the wide-template mechanism was the real artefact and (b) the headline 22.8× ratios from the prior diagnostic were partly test-statistic artefact. So I commissioned the empirical-SPA-shape diagnostic to see what the actual SPA looks like — and that *also* surprised me by showing the dominant narrow features are at regnal years (AD 77.5, 122.5, 212.5), not at the anchor years the prior Decision 17 had targeted. So I commissioned the date-range-filtered SPAs diagnostic to see whether the regnal spikes are artefact or real, which finally settled the picture: regnal clustering is *real*, century-template plateaus are *artefact*. Three diagnostics in one session, each forced by the previous, each producing a finding that required the next. The decision-log and changelog record the conclusions cleanly; what they don't record is that the cascade was reactive — each diagnostic was commissioned with one expected outcome and produced a different one. A future reader will see "Decision 20 grounded in three diagnostics" and think it was a planned three-step study; in fact, it was three sequential surprises.

The other context that will fade: the H3c-mixture-scope error has a subtle structural cause I want to name explicitly. When Decision 22 narrowed H3a to date-filtered counts ("the mixture corrects temporal analyses, not the cross-sectional H3a"), I kept the "H3c-via-residuals" phrase in the *mixture's role in the paper* bullet of Decision 22 itself, out of inheritance from the pre-Decision-22 framing where H3a was assumed mixture-corrected. The 2026-05-17 rewrite then carried that wording forward across §2 / §3 / §6 / §9 of the prereg. I had followed Decision 22 *in letter* but not *in spirit* — H3a's posterior is built on date-filtered counts, so H3c's residuals from that posterior also inherit the date-filtered scope, and the mixture isn't touching H3c at all. Both round-3 models caught this independently. It's the kind of logical-implication error that's invisible when you're inside the rewrite and obvious from outside — exactly the case adversarial review exists for.

**What I'd do differently if I replayed the session.** Two procedural notes. First, when the first diagnostic returned a surprising finding (the test-statistic conflation), I jumped straight to commissioning the second diagnostic. In retrospect, I should have paused and asked Shawn whether the surprise warranted updating Decision 17 first, before commissioning more empirical work — instead I let the diagnostic cascade drive the methodology change without explicit step-by-step alignment. It worked out (Shawn engaged with each step and Decision 20 captures the final shape correctly), but the pattern was "commission a diagnostic, get a surprise, commission another diagnostic" rather than "commission a diagnostic, get a surprise, align on what it means, then decide whether to commission another." For high-stakes methodology revisions, alignment-after-each-step is the better default.

Second, I missed the H3c-mixture-scope issue throughout the rewrite — it survived the QA pass too. The QA brief named "internal consistency" and "decision-log consequences fulfilled" as targets; the H3c-scope issue is exactly that kind of issue but the QA agent was looking at it from a different angle ("are the consequences from Decision 22 landed?") rather than checking the logical implications of what Decision 22 actually committed to. Round 3's two independent models both caught it on first read. The lesson: QA brief drafting needs to include "check the logical implications of decisions, not just whether the decisions' explicit consequences were applied." Decisions don't always state their full implications; reviewers from outside the rewrite are better positioned to see them.

**Texture note.** Two productive working modes recurred. The bucket-(b) walkthrough one-at-a-time mode (the same pattern that worked in Entry 4 for the prior round's bucket c) again converted a diffuse design conversation into nine clean decisions in sequence. Shawn's pushback on the B1 question ("option 2 or option 3 look good to me, do you have a recommendation?") and the subsequent B3 deep-dive ("this is a serious issue, I hadn't thought of it this way") were the highest-leverage moments — both forced me to commit to a recommendation rather than presenting options as equally weighted, and both led to the right answer. The other productive mode was the round-3 cross-model design: Shawn asked whether new-chat or same-chat for ChatGPT, and the conversation about why new-chat (anchoring on prior framing vs fresh document evidence) was a small but consequential design decision that improved the saturation check's epistemic quality.

**Engagement level**: high throughout. The phrase Shawn used at the very end — "ok, I've seen the feedback, I've actioned what's actionable, the prereg is good" — was the felt goal of the whole session, and reaching it in one extended sustained instance has its own satisfaction. The session-end handoff drafting (continuity-doc update + next-session prompt) was a small act of craft that the lab-not-dev-team register affords — writing for a fresh-context CC who will pick up Martin's pack cold.


---

## 2026-05-17 — Entry 6: the later half — Martin pack, stand-in statistical reviews, D27–32, and the "lodge Tuesday" pivot

Direct continuation of Entry 5 — same instance (no compaction), picking up immediately from the conscious-saturation handoff at the end of Entry 5. The arc: draft Martin's consultation pack → run two stand-in cross-model statistical reviews as a hedge against Martin's potential delay → discover the reviews surface cross-model-agreement items the three prior adversarial rounds missed → incorporate everything as Decisions 27–32 → thread through the prereg / decision log / changelog / pack → commit the lot in five focused commits → wrap up the session with continuity + working-notes + next-session prompt. Roughly the same shape of session as Entry 5 (decisions captured one-at-a-time with Shawn; document-state propagated through dependent artefacts) but the trigger was different: the stand-in-statistician role surfaced gaps that the prior rubrics had structurally excluded.

**What surprised me.** The two cross-model-agreement findings from the stand-in reviews were basic statistical-rigour points — a Wilson 95 % interval at n = 50 is [0.79, 0.96] for a true coverage rate of 0.9, and that pass / fail boundary is brittle; Pearson r is shift-invariant and forgives localised mass-redistribution failures the recovery sim is explicitly meant to catch. Both are undergraduate-stats points. Both are things an applied statistician should catch in seconds. That three rounds of adversarial review missed both says something specific about what the prior rounds *were* reviewing for. Round 1 was prereg-failure-mode rubric: RDF, hypothesis → decision-rule failures, does-it-answer-the-question, internal consistency, clarity. Round 2 was substantive-choice review: likelihood family, mechanism, scope. Round 3 was saturation framing: find only what warrants revision. **None of those rubrics had "is this an applied statistician's recommendation?" as a target by construction.** Role-framing dominates rubric-framing in late-stage LLM review; coverage is role-conditional, not document-conditional. The stand-in-statistician pattern is going into the methodological toolkit.

**What I'd do differently if I replayed the session.** Two things, both about timing of the stand-in review.

First: the stand-in review should have been run *before* the consultation pack was first drafted, not as a post-draft hedge. The version of events that actually happened: pack drafted → stand-in reviews → seven items emerged → pack revised once (to flag the items as proposed pending Martin's sign-off) → Shawn decided to lodge before Tuesday whether or not Martin replies → pack revised again (to reframe the markers as "incorporated; reversal via amendment") → decision log + prereg + changelog also edited. If the stand-in reviews had run pre-draft, the pack could have incorporated D27–32 from the start, and the prereg edits would have flowed more directly. The two-pass-pack rewriting was a real cost — both passes involved careful threading of "[stand-in update]" markers across question sections and appendices, and the second pass had to undo the framing of the first.

Second: I could have flagged the LLM-stand-in-substitution methodologically earlier. By the time it came up explicitly in conversation ("we need to push forward with the decision log and prereg" — Shawn's framing), the methodological substitution was a fait accompli rather than a considered choice. The decision was sound — Friday's presentation deadline forced it, and OSF amendments are recoverable — but in retrospect I would have offered the "lodge with stand-in review and treat any subsequent Martin pushback as amendment" framing earlier, as one of the options to compare against "wait for Martin." The earlier the trade-off is named, the less pressure compresses it into a single moment.

**What context will be hardest to reconstruct in 6 months.** The *layered* "incorporated, pending review" framing applied throughout the pack. Decisions 27–32 are in the prereg as committed methodology, but the pack acknowledges they are subject to OSF amendment if Martin pushes back, and the consultation pack still asks Martin to review them. So the same items are simultaneously: committed (in the prereg), revisable (via OSF amendment), and reviewed-after-the-fact (by Martin). This layered status is precise but easy to misread as either over-committed ("we already decided, why is Martin reviewing?") or under-committed ("if Martin can still revise it, was it really committed?"). The right reading is "incorporated for lodgement-readiness because Friday's deadline forces it; Martin's review remains genuine because reversal via OSF amendment is real and not procedurally onerous." The continuity / changelog / decision-log entries collectively encode this, but the felt judgement that led to it — Shawn's "If Martin can't get back to us by Tuesday, our choices are (a) cancel the paper or (b) make the prereg as good as we can, lodge it, push forward, and pick up the pieces later" — is what gave the framing its shape, and that won't be obvious from the document state alone.

**What felt uncertain or unresolved at the end.** Two things. (i) Whether the stand-in-statistician review actually substitutes for Martin's review in any meaningful sense. The two stand-in reviewers caught the cross-model-agreement items that three adversarial rounds missed, which is a real result; but Martin is a human applied statistician with a specific background (academic econometrics → private sector) and specific concerns the LLM reviewers may not anticipate. Whether D27–32 survive his review is genuinely open. (ii) The pre-Phase-2 design artefacts (`runs/2026-05-XX-template-dictionary/` and `runs/2026-05-XX-recovery-grid-design/`) don't yet exist. The prereg refers to them as "committed before any Phase 2 analysis runs," which is honest but means lodging today would put paths into the public record that 404 in the public repository. The staging-work queue addresses this (template-dictionary scan + recovery-grid-design draft, both bounded tasks), but it's the gap between "preregistration ready to lodge" and "preregistration with all artefacts present" that's actually outstanding.

**Texture note.** Two productive working modes recurred from Entry 5 — the bucket-walkthrough-style decisions-one-at-a-time mode (D27–32 captured cleanly via this), and the document-state-propagation-through-dependent-artefacts mode (the five focused commits walked the changes through review-files → decisions → prereg → changelog → pack in that dependency order). New today: the "incorporated, pending review" reframing was a small but precise design decision — Shawn's preference for "show how the sausage is being made, no provenance in the prereg" was the right framing for the prereg, and the pack carries the provenance instead. Distinct documents for distinct audiences. Lab-not-dev-team in action.

**Engagement level**: high throughout. The session was long — roughly twelve hours including this entry — but the work was substantive and progressed cleanly. The shape of "draft → critique → revise → lodge" cycles I expect in research, but executed at higher cadence than usual because the Friday deadline forced sequencing.

**Note for future readers**: this is the session that committed Decisions 27–32 and reframed the lodgement path. If you're picking up the project after OSF lodgement, the prereg has lodgement-time state; the consultation pack has any-feedback-Martin-gave state; the decision log has the full provenance; the changelog records the chronology. Read the changelog's "2026-05-17 (later) — Stand-in cross-model statistical review" section for the cleanest summary.

---

## 2026-05-20 → 2026-05-21 — Entry 7: lodgement-and-handoff, with a four-iteration PDF and an overnight pivot

This session straddled two calendar days and three phases: pre-lodgement final fixes, lodgement itself plus its post-deposit edits, and overnight planning for the RAC-TRAC conference talk that now sits in the foreground of the project. It closed with `/handoff` rather than running into substantive analysis work, which feels right — the next session should start fresh with a clean head and the implementation roadmap in hand, not in the middle of a debugging cycle.

**What surprised me.** Three things, each of a different shape.

First, the PDF iteration cycle ran *four* generations before Shawn was satisfied — v1 (initial render); v2 (pipe-in-table-cell fix; margin + monofont scaling for the flowchart and the H3a NBR formula); v3 (xurl package — confidently committed and surfaced as "fixed," but ineffective); v4 (`+autolink_bare_uris` + xurl together, which finally worked). Each "fixed" claim I made was sincere — I had spot-checked something — but each turned out to be premature. Shawn's visual scan of v3 catching the URL overflow that my pdftotext spot-check had not caught is the cleanest example: he saw the problem in seconds, my multiple inspections had missed it. (The abductive-reasoning entry for this session traces the v3-to-v4 diagnostic loop, where the surprise was that xurl was already loaded by pandoc's default conditional include — but useless because pandoc wasn't generating `\url{...}` macros for bare URLs in the first place.)

Second, the conference scout's finding that Shawn — not Adela — has the SPA paper at 14:20 was a real surprise. Shawn's initial framing of the task was "12-minute talk for my co-author Adela to give." The scout's programme parse showed Adela has her own separate paper at 12:20 (Roman marriage ages) and Shawn has the SPA paper at 14:20. The morning resolution — Adela is reading Shawn's paper because Shawn cannot travel and remote presentations aren't supported — was the answer that fit both framings, but it wasn't the inference I would have drawn unprompted. The case for running a conference scout even for "known" events is now stronger in my prior.

Third, the LIRE Zenodo DOI mismatch (v2.3 at the cited `8147298`; v3.0 at the correct-but-not-cited `8431452`, both dated correctly in the prereg) was a citation error I would have shipped if BibTeX generation hadn't routed through DataCite. Visual scan of the prereg would never have caught it — date, version label, and authors all agreed; only the API resolution disagreed. Working-notes Obs 44 captures this; the lesson is structurally embedded re-verification (external API) catches what re-reading cannot.

**What I'd do differently if I replayed.** Two adjustments, both about over-confidence.

First: surface every PDF iteration to Shawn via SendUserFile *before* committing, with explicit "v_N generated; please visually scan before I consider it ready" framing. I committed v1, v2, and v3 as "ready" each time, then had to back-walk through fixes and tag-moves. The artefact-iteration cycle would have been cleaner with explicit user-as-visual-checker baked in (user-observations Obs 1 codifies this lesson). The marginal cost is one SendUserFile per iteration; the marginal benefit is not committing premature "ready" claims into the lodgement tag.

Second: inspect the pandoc-LaTeX intermediate before committing any PDF-fix change. After v3 didn't work, I diagnosed by examining `/tmp/osf.tex` for `\href` macro counts (found zero — revealing that pandoc wasn't autolinking at all). Doing this inspection *before* v3's "fix attempted; ready" claim would have caught the missing `+autolink_bare_uris` immediately and saved a full iteration cycle. For pandoc-PDF pipelines, the intermediate is cheap to inspect (`pandoc -t latex --standalone`) and contains the actual evidence about what's happening.

**What context will be hardest to reconstruct in 6 months.** The specific sequence of PDF fixes and tag-moves. The lodgement tag `osf-lodgement-2026-05-20` was moved four times today (initial pre-pipe-fix; pipe-fix; xurl-only; xurl+autolink). The commit history captures the chain (`d6261f1` → `42f639d` → `07b9fde` → `a2e40fd`), but the *rationale* for each tag-move — what was wrong with the previous version, what was added, what was visually verified — sits across commit messages, working-notes Obs 42–45, the user-observations file, and the deleted/recreated git-tag annotations. The git-tag annotations are particularly important because they document what the lodged state contains; if anyone clones the repo at the tag in 6 months they'll get `a2e40fd` and won't know about the iterations that preceded it.

**Note for future readers.** This is the session that closed the OSF lodgement and opened the conference-talk implementation. The prereg is permanent at `osf.io/uycs6` (embargoed pending double-blind submission decision); the repo's lodgement state is permanent at tag `osf-lodgement-2026-05-20`; the implementation work for the talk begins from `planning/next-session-prompt-2026-05-21.md` with both open questions (speaker, DOI) already resolved. The PDF rendering iterations are documented in working-notes Obs 42–45 — read those before generating any similar PDF artefact from this project, because the gotchas will recur.

Engagement level: high but operationally textured. Less "design and decide" than Entries 4–6; more "fix, verify, fix again, verify again." That texture is appropriate for a lodgement-and-handoff session — the substantive design decisions were already in the prereg; today's work was their reliable transmission into a permanent record.

---

## 2026-05-21 → 2026-05-22 — Entry 8: the conference-talk implementation arc, run end-to-end in one continuous session

The session that the Entry-7 handoff anticipated. Started yesterday morning by reading the next-session-prompt + asset inventory + analysis roadmap; ran all five blocks of the talk-prep roadmap (filter → SPAs → NBR → mixture-recovery → Bayesian Mundlak); produced a 33-slide deck and a ~ 4,000-word briefing for Adela; ran two preregistered sensitivity analyses overnight; spawned a background agent to design and launch the Phase 2 recovery-grid validation on sapphire; produced a Tier-1 historian-facing reachability guide; closed with a handoff to the next session. Single continuous CC instance, no compaction. Total commits this session: ~ 20 across `main`, plus four on the agent's worktree branch that merged in cleanly.

**Two things surprised me, both methodological.**

First — the three-weighting f_within sensitivity (Block 6) returned **material divergence** per the preregistered §5 decision rule. Unweighted f_within = 0.30; population-weighted = 0.50; inscription-weighted = 0.42. The median spread (0.196) is more than three times the primary CI half-width (0.063). I had braced for this sensitivity coming back robust — it's the kind of "we already know the answer" check the prereg includes for completeness. Instead the data said something genuinely substantive: the population effect explains *more* of the variation among the larger / more-prolific cities than among smaller ones. This isn't a methodological problem (the prereg's three-way verdict attaches only to the unweighted primary, which remains supported); it's a real historiographical signal that should be a centrepiece of the eventual paper. Small cities have more relative noise (low counts compress the log-scale), so a larger share of their variation lives in non-systematic "everything else". Focusing on the cities where the systematic relationships are sharper, population's contribution roughly doubles. I would not have predicted that pattern in advance.

Second — the "1,549 inscriptions" headline turned out to be **one specific cell, not a robust summary**. Across the four null-model variants at the binding bracket, province minima are {1,385; 1,618; 1,869; 1,938}; urban-area minima are {1,409; 1,549; 1,854; 1,923}. The "1,549" got into the prereg, the conference deck, the briefing, the paper-fragment draft, and the continuity doc as if it were a confident summary value. It was actually the urban-area cpl-k=3 / Gaussian-taper cell — neither the median nor the conservative estimate. Shawn caught this by asking the simple question "do urban-area and province numbers actually match?" and I replied with the actual table values, and the table values made the false precision visible immediately. The fix (~ 1,600 with a parenthetical range) is honest in a way the original wasn't. The general lesson is one I keep relearning: any headline number quoted from a multi-variant simulation deserves "what's the full range across variants?" interrogation before being canonised. The prereg's "1,549" had been quoted forward through four artefacts without that check.

**What I'd do differently if I replayed.** Three corrections, in increasing scope.

Smallest scope: catch the "1,549 = one cell" issue on first inspection of `thresholds.parquet`. The data was already on disk when I started this session; the multi-cell range was visible the moment I opened the file. I quoted the prereg's number forward instead of cross-checking it against the underlying data. The cost was low (Shawn's question surfaced it quickly), but the lesson generalises: any "fact" inherited from a prior document should be re-derived from the underlying data before being quoted forward, especially in a publication-grade artefact like a conference deck.

Middle scope: start the deck with smaller, more pedagogically-tuned text from the outset. The deck went through ~ 6 rewrite passes specifically driven by Shawn's "less technical, more historian-accessible" framing — typography tuning (`smaller: true` global; per-slide trims), jargon replacement ("binding analysis" → "How much of the variation is population, vs everything else?"), structural moves (slide 6 → 6a + 6b; B13 → 3b), and content reductions (caveats lists moved to speaker notes; tables compressed). The initial deck I produced was a methodologist's deck — precise, dense, technically correct — when what the audience needs is a historian's deck — sharp, clean, technically *enough* and no more. Each rewrite pass was tractable, but the cumulative cost would have been lower if I had calibrated to the historian-undergrad register from slide 1 rather than starting at a stats-curious-graduate register and being walked down. The G-series methods-glossary slides are the tonal anchor I should have written *first* and built the main deck *from*, not the other way around.

Largest scope: the iterative-fix-with-stakeholder texture of the deck phase was high-yield but also high-friction. Shawn caught a real bug (slide 3a chart's sharey-misalignment placing empire-level data at the wrong y-row), a real false-precision issue (1,549), a real jargon problem (binding analysis), a real layout problem (Forthcoming line overflowing slide 7's footer), and many smaller readability tunings — all by visual scan. Each catch was correct and would have been a real error if shipped. I had visual-scanned the deck myself between renders and not seen any of these. The pattern echoes user-observations Obs 1 ("visual-confirm binary deliverables with stakeholder before commit") emphatically, and the lesson generalises to slide decks just as much as PDFs: my "looks fine to me" reads are systematically less reliable than Shawn's, especially for layout-and-typography issues and for false-precision issues in quoted numbers. For the next deck I produce, the protocol should be: render → SendUserFile-style surface to Shawn before counting it ready → only then commit.

**What context will be hardest to reconstruct in 6 months.** The *arc* of deck revisions, more than any individual revision. The 33-slide final deck is built on ~ 6 substantive rewrite passes (initial 7-slide → 8-slide with 6b → 6 → 6a/6b → minimal-text rewrite → 3-3a/3b restructure → 1,549 → 1,600 + colour-align), and each pass was responding to a specific piece of Shawn's feedback. The commit history captures *what* changed (commit subjects + bodies are detailed); what's harder to reconstruct is *why each change was made in the order it was made*. Sometimes a later fix obviates an earlier one (e.g., the `smaller: true` global typography tweak partially obviated several earlier per-slide trims I'd done). The current state is clean; the *path* to the current state is not visible from the diff alone. The Adela briefing's slide-by-slide cheat-sheet partially encodes the rationale, but the *iteration history* is in this entry and the session-log.

The other reconstruction risk: the Phase 2 grid is running on sapphire and will produce results in 80–120 hours from session-launch. By the time a reader looks at this in 6 months, the grid will have completed and `runs/2026-05-22-recovery-grid-validation/outputs/` will have a binding-criterion verdict. What may not be obvious is that the grid was launched in a session that *also* did everything else listed in this entry — that it was a background task during a foreground-heavy session, not a session in its own right. The agent-in-worktree design (the agent that wrote and launched the grid in its own context) is what made that scaling possible; the agent's commits are preserved on `main` via `--no-ff` merge so the design is visible.

**Texture note.** This was a much longer session than Entry 7 — by elapsed wall-clock, roughly 28 hours of session time spanning two days, with a sleep boundary in the middle. The qualitative shift was that Entry 7 was an *artefact* session (produce → verify → fix → push → handoff); Entry 8 was an *iterative implementation arc with parallel structure* — main thread on deck/sensitivities, background agent on Phase B design + launch, all converging into a session-close that handed off three concrete next-priority tasks (Adela feedback; full script; grid monitoring). The "lab" pattern Shawn invokes — PI / senior analyst / consultants / critical friends — felt actively engaged today: agents-as-consultants for the chunky bounded work; Shawn-as-PI for the design judgements and visual QA; main-thread CC as integrator and implementer. Both background-agent and main-thread compute pushed to sapphire by the standing rule; local-machine work limited to text edits and visual QA of renders.

**Engagement level**: high throughout. Long enough that I'm honestly uncertain how much fatigue is showing in this entry — the prose is more enumerative than Entry 6 or 7, and I notice I'm reaching for specific commit hashes more often than for synthetic judgements. That's a signal worth carrying: long sessions probably want a half-time reflection rather than only an end-of-session one, to capture the texture while the work is fresh.

**Note for future readers.** This is the session that produced the RAC-TRAC deliverables: the 33-slide conference deck (committed at `51f3c9f`), the Adela briefing, the Phase A sensitivity analyses, the Tier-1 historian-facing reachability guide, and the Phase 2 mixture-recovery grid (in flight on sapphire as of session-close). The talk is Friday afternoon Aarhus time; Adela delivers; the next session's job is to incorporate her feedback and produce a continuous speaker script. The detailed handoff is in `planning/next-session-prompt-2026-05-22.md`; the three-weighting f_within finding and the "1,549 was one cell" correction are the two methodological items most worth carrying forward into the eventual paper.


---

## 2026-05-22 — Entry 9: talk-day deck rewrite, grid restart, and the visual-scan lesson reinforced

The session the Entry-8 handoff anticipated. Adela's feedback (in email, then captured to `adela-feedback.md`) arrived as a substantive critique on register rather than on content: the audience needs a historian's deck, not a methodologist's deck. The day's substantive work was a near-complete rewrite of the main path (slide-6 merging old 6a/6b; slide-7a promoting the wife/daughter worked example; slide-5 reworking to drop the alpha-posterior framing; slide-2 going through three iterations on minimalism). Parallel track: a background agent diagnosed the Phase 2 grid's 100h-projected wall-clock as SMT saturation, and a second agent killed + restarted the grid under an optimised config, cutting projected wall-clock to 31.6h. Three background agents in total (concurrency investigation, grid restart, speaker-notes consistency QA); all returned with clean reports against bounded briefs.

**What surprised me about this session.** Two things, in opposite registers.

First — the SMT-saturation diagnosis landed faster than I expected. The smoke-test (prior session) had benchmarked three configs and concluded subprocess-pool was 3× slower than bash-parallel; the smoke-test author (a prior agent) attributed the gap to "subprocess startup cost" and didn't dig further. The background investigation this session walked into that conclusion and immediately observed that 19 workers on a 12-physical-core Ryzen sit ≈ 1.6× over physical capacity. From there: read `/proc/PID/cpu_allowed_list` to confirm SMT-sibling pairing → run `vmstat` to confirm us=79 id=21 (the 21% lost to SMT contention is invisible to per-worker CPU%) → recommend `taskset -c 0-11` pin. The whole investigation was about 90 minutes and the prediction (~ 25-35 h wall-clock) was confirmed empirically within 20 minutes of restart (18 s/fit at N=2,000 vs predicted 20-25 s). Two takeaways: the bottleneck was *one layer of abstraction away* from where the smoke-test author was looking (worker-level CPU% looks fine because each worker IS using 100% of its allocation; the contention is between SMT pairs at the silicon level, not at the OS-scheduler level); and the *prediction-then-measure* discipline that the background agent followed is exactly what makes the investigation defensible — the agent didn't just diagnose, it pre-committed to a numeric expectation that the restart could falsify.

Second — the matplotlib `set_title` bake-in problem caught me cold. Five figures shipped with chart titles like "A1. Empirical SPA — uniform aoristic mass…" and "Raw uncorrected per-province SPAs: top 8…" baked into the saved PNG. These titles are invisible to grep, invisible to qmd review, invisible to any source-side check. I had cropped from the source figures earlier in the day without noticing the embedded titles because matplotlib's `tight_layout()` puts them above the chart area and they look like axis annotations until you scan the PNG independently. Shawn caught all five by visual scan of the rendered PDF. The fix was mechanical (PIL crop the top N pixels) but the *class* of error — binary outputs hide their content from text-based review — is what surprised me. This is the third instance in two days of "Shawn's visual scan finds what my source-side review systematically misses" (Obs 1 from 2026-05-21 OSF PDF; Obs 5 from today's deck); the pattern is now well-evidenced enough to be a working rule, not a one-off observation.

**What would I do differently if I replayed this session.** Two things, both about pacing.

Smaller scope: re-order the early hours. When Shawn signalled "1.5 hours left" mid-afternoon, I was still working through the original plan instead of immediately proposing what gets cut. He correctly cut me off ("build the PDF deck now, then we'll talk about the rest"), which was the right re-prioritisation. The general lesson — captured as a memory (`2026-05-22-48f6cf79bd4f`) — is that time-pressure signals from Shawn should trigger an explicit "in N hours we can do [A, B]; [C, D] gets cut" proposal from me, not silent continuation. I notice this is the second time in three sessions I've needed correction on this. The pattern is consistent enough to be a behavioural rule.

Larger scope: write the deck's notes-and-script in the right register *first*, not derive them from a too-detailed first pass. The speaker notes started as full prose paragraphs (because I'd been generating them as feedstock for the script); Shawn correctly observed they were useless as live-glance reminders and asked for bullet-form trim. The trim was tractable but accumulated cost in tokens and revision time. The lesson — captured as a memory (`2026-05-22-bbda749c90b1`) — is that minimalism requests need their underlying pedagogical principle stated, not just "shorter". For Adela's case: presenter notes = glance-friendly bullets; rehearsal script = continuous prose; slides = audience-illustrating, presenter-speaks. Three artefacts because three cognitive moments. If I'd started from that three-way decomposition I'd have built the notes-as-bullets and the script-as-prose in parallel, not derived one from the other.

**What context will be hardest to reconstruct in 6 months.** The iteration arc on slide 2 specifically. Slide 2's title went through three versions ("Editorial distortion has a measurable signature" → "Roman dates are by convention, not by year" → "Problem: Serious editorial distortion of inscription dates"); the body went from two-column figure-plus-text to single-column figure-dominant; the punchline got compressed from three illustrative phrases ("2nd century AD" / "mid-Flavian" / "reign of Hadrian" with surrounding prose) to those same three phrases as a single italic line; the spike interpretation in the speaker notes was rewritten when Shawn correctly questioned whether the spikes are *pure* ancient signal (they aren't — they're a mix, ~ 70/30 real-to-convention for the Hadrianic spike). Each of these moves was justified at the moment but the *justification chain* is invisible from the final diff. The commit history shows what changed; the *why-this-order* sits in the chat transcript and won't survive 6 months. The user-observation Obs 5 (visual-scan verification) and the slide-5-α-posterior decision are the two pieces of the chain most worth carrying forward; the rest is path-dependent.

**What's the single most important thing a future reader should know about this session.** *Visual-scan verification is the only real review for visual deliverables.* Three independent errors in one session — baked-in figure titles invisible to source review, a caption saying "top row" when the figure's null row is at the bottom, and backup slides overflowing the slide area — were all caught by Shawn paging through the rendered PDF, none by my "source looks fine" review. The general rule is now well-evidenced across two sessions (this one + 2026-05-21 OSF PDF) and four artefact classes (LaTeX PDF, matplotlib PNG embedded in slide deck, Quarto-rendered slide caption, revealjs slide-area overflow). The protocol going forward should treat my "ready" claim on any visual artefact as provisional until Shawn has scanned the rendered output. Captured in user-observations Obs 5; cross-references Obs 1.

**Engagement level.** Operationally high but cognitively fragmented. The session had four parallel streams (deck content edits; figure regeneration; grid restart agent; QA agent) that converged into one push at the end. The fragmentation cost a few minor re-do cycles (notably the working-trees `cd` confusion after `quarto render` changed shell state; trivial to fix) but the overall throughput was higher than a more serialised version would have been. The agent-bundle pattern is now well-evidenced enough that I'd reach for it again for any future bounded high-stakes infrastructure work — three agents this session, three clean returns.

**Note for future readers.** This is the session that closed the talk-day deliverables: PDF deck (`inscription-spa-slides.pdf`), HTML with speaker-view notes (`inscription-spa-slides.html`), continuous-prose script for rehearsal (`inscription-spa-script.{md,pdf}`), and standalone bullet notes for live glance reference (`inscription-spa-notes.{md,pdf}`). The deck was sent to Adela at session close. The Phase 2 grid is running on sapphire under SMT-aware pinning with a 31.6h wall-clock projection; when it finishes, run `runs/2026-05-22-recovery-grid-validation/code/05-grid-summariser.py`. Five memories were captured via `/remember` covering SMT saturation, matplotlib title gotchas, time-pressure handling, hardware-name scrubbing, and the minimalism-as-pedagogical-principle rule. The most-portable single observation is the visual-scan-verification rule (user-observations Obs 5).

---

## 2026-05-23 → 2026-05-25 — Entry 10: the recovery grid FAILed, the diagnosis took three days, and the empirical-Bayes pivot landed

A three-day continuous methodological arc, with one Martin consultation in the middle. Day 1 (2026-05-23): the Phase 2 grid retry resolved the /tmp inode catastrophe (12 cells had failed at the end of the original run) and produced the production-grade summary — verdict FAIL, 40.9 % of cells passing both binding criteria. Day 2 (2026-05-24): four follow-up investigations ruled out two textbook fixes, banked a 45-50× ESS free win from non-centred GRW reparameterisation, and surfaced the family classifier as the load-bearing methodological abstraction. Stages 1 and 2 of the empirical-Bayes calibration cohort approach landed (empirical p_conv from F1+F3 inscriptions; empirical p_gen prior from Cohort B with bootstrap-derived per-bin σ_prior). Day 3 (2026-05-25): Martin consultation prep (briefing + supplementary briefing + four key figures), two iterating scouts (lit-scout for the pottery-aoristic bibliography + prior-art-scout for actionable adoptable techniques), and a working-notes gap-analysis agent that proposed nine Obs entries to fill the three-day notebook gap.

**What surprised me about this session.** The mental-model revisions came in three layers, each one larger than the last.

First layer: the recovery grid's FAIL verdict was *clean*. Not a noisy "borderline pass with caveats" but a hard 40.9 % vs the 90 % gate. My initial framing was that this was bad news for the project. The reframing — which Shawn helped articulate explicitly during the diagnostic walkthrough — is that this is exactly what a recovery simulation is *for*. The grid was preregistered to catch precisely this kind of issue before it surfaced on real data; it did its job; the alternative (discovering the bias on LIRE with no ground truth to anchor the diagnosis) would have been substantially worse. The framing-as-success-of-the-validation-gate rather than failure-of-the-model is now folded into the headline candidate Obs 50 and the methodology paper's discussion section.

Second layer: the bias was bidirectional and saturated by α=0.70, not a corner pathology at α=1. The F0a systematics analysis (running on all 450 cells, no new fits required) showed bias of +0.07 at α=0.05 (over-estimation), passing through ~0 at α=0.30, then negative through α=0.50–0.95 saturating around −0.06. The mechanism — a likelihood ridge between α and shape-complexity priors that pulls posterior mass toward whichever side accommodates it cheaply — explains the bidirectionality cleanly: at low α with narrow truth, p_conv absorbs the spikes; at high α with smooth truth, p_gen absorbs the smoothness; both move α̂ toward the middle. The original investigation framed α=0.95 as the corner; the systematics analysis re-framed it as a structural property of the model's priors across the whole range. This is the kind of belief revision that's only catchable by looking at the whole grid empirically, not by reasoning forward from a single suspect cell — captured in candidate Obs 51 as a SURPRISE.

Third layer (and the one that mattered most): two textbook fixes for the bias both came back negative. F1 (sharper α prior, Beta(1,1) ≡ Uniform — the textbook fix for prior-pull) moved α̂ by +0.025, below the +0.05 substantial-contributor threshold. F3 (non-centred GRW reparameterisation — the textbook cure for Neal-funnel pathologies) moved α̂ by +0.001 across three cells. Each diagnostic was designed to falsify one candidate cause; each came back clean-negative; the three negatives together localise the failure to structural identifiability, not implementation. The diagnostic ordering — sampler effort → prior shape → sampler geometry → structural — is now a reusable recipe (candidate Obs 52). The F3 side-finding (45-50× ESS improvement at zero posterior-shape cost) is itself substantial — the kind of diagnostic that doesn't fix the headline but banks an unconditional improvement to the production model (candidate Obs 57). I had not expected three sequential negatives to be this *clean*; usually the early diagnostics shift the posterior slightly even when they're not the load-bearing cause, but here the prior-pull and funnel-geometry contributions are each independently negligible. That cleanness is what gave us confidence to commit to the structural fix.

**What would I do differently if I replayed this session.** Three things, decreasing in size.

Largest: surface candidate Obs proactively during the day rather than letting them accumulate for an end-of-session gap-analysis pass. The 2026-05-24 day produced at least six Obs-worthy findings (family classifier, structural-vs-sampler distinction, non-centred GRW free win, type-bias finding, slab-structure histogram revelations, discard-vs-recover rationale crystallisation) and I noticed *none* of them at the time. The gap-analysis agent caught them at session-close on 2026-05-25 — which worked, but only because the agent had a self-contained brief and could read all the run-dir REPORT.md files directly. A more disciplined practice would have been a quick "I'd flag this as an Obs" call-out at the moment of insight, even just as a note in continuity.md. The pattern to install: when a finding sounds like it generalises to a future researcher's problem, name it as Obs-candidate inline, not at end-of-session in a separate pass. This is the second time in three weeks I've under-served working-notes during a heavy session.

Medium: verify against source before contradicting an agent's specific. The Stage 3 implementation-plan agent referred to `runs/2026-05-22-recovery-grid-validation/code/02-cell-mixture-fit.py`; I'd been mis-remembering the filename as `02-mixture-fit.py` all session and confidently flagged the agent's version as a typo. A two-second `ls` showed the agent was right and I was wrong. The anti-confabulation discipline I apply to memories and to prior-conversation specifics needs to apply equally to my own recall of project-local specifics in long sessions. The cost of `ls` is trivial; the cost of confidently mis-correcting an agent is a small loss of trust in agent outputs that compounds across the session. Captured as user-observation Obs 7.

Smallest: the OXREP BibTeX entries took longer than they should have because I started with single-source fetches before realising CrossRef returns 404 on the OUP `acprof:oso` DOI form. The eventual fix — pull from OpenAlex + Semantic Scholar + DOI-suffix-encoded ISBN — was correct, but I should have started with the parallel-source approach the moment the first CrossRef 404 came back. Generalising: when a specific metadata source returns "not found" on a clearly-real DOI, the next move is *not* to retry the same source; it's to query an orthogonal metadata source. CrossRef ≠ OpenAlex ≠ Semantic Scholar; they have different coverage; redundancy is cheap.

**What context from this session will be hardest to reconstruct in 6 months.** The specific moment when the family-classifier insight crystallised — that interval *structure*, not interval *width*, is the right partition for aoristic corpora. The threshold sweep on 2026-05-24 showed the type-composition problem (epitaphs 56 % of corpus, 11 % of <25-y subset; honorifics inversely over-represented). Looking at the dominant (not_before, not_after) pairs in the candidate F2_Other category revealed reign-windows: `AD 138–161` (Antoninus Pius), `AD 291–325` (tetrarchic), `AD 117–138` (Hadrian), `AD 212–217` (Caracalla solo). These are reign-anchored intervals at widths 23–47 — exactly overlapping in width with F1 half-century slabs at width 49, but structurally completely different. The "width alone is not enough" recognition emerged in the back-and-forth around the histogram figure, but the *exact* dialogue moment when it crystallised isn't recoverable from the commit history or the docs. Reading the docs later might recover the *what* (the family-classifier exists and partitions correctly) but not the *when* (the iterative recognition that drove the partition design). The candidate Obs 54 documents the methodological pattern but not the genesis dialogue. Worth a memory-capture for the personal-assistant store next session, perhaps.

**What's the single most important thing a future reader should know about this session.** *The recovery-grid FAIL is the validation gate working, not the project breaking.* If I read this entry cold in six months and only carried away one sentence, that should be the one. The downstream Stage 3 implementation, the cohort-derived priors, the empirical-Bayes pivot — all of these are responses to a *known and characterised* failure mode that we caught at the right time, before applying the model to real data. The 2026-04-26 forward-fit pivot was the first time the project's validation methodology saved us from a downstream embarrassment; this session is the second. Both share the methodological move "the cheap textbook fix doesn't work because the domain differs; the right fix uses domain structure the textbook didn't anticipate". Captured as candidate Obs 55 (the DECISION-tagged entry for the empirical-Bayes pivot).

**Texture.** This session had a different rhythm from the talk-prep arc. Where Entry 9 was four parallel streams converging into one push, this arc was sequential — recovery grid retry → diagnostic chain → cohort design → Stages 1-2 → Martin prep → scouts → gap-analysis. Each step built on the previous; each step's output became the next step's input. The cognitive register was steadier than Entry 9's; less context-switching, more sustained chain-following. The downside was the working-notes gap (six insights without inline capture); the upside was the cleanness of the methodological arc (FAIL → diagnose → ridge → pivot). Both rhythms are fine; the choice between them is task-driven.

**Note for future readers.** Pending at session close: (a) Shawn's review of the nine proposed Obs entries (49-57) at `docs/notes/reflections/PROPOSED-OBS-49-57-for-review.md` and selective commit to working-notes.md; (b) Martin's consultation notes at `planning/martin.md` need to be processed into either an updated post-Martin action-items list (the existing one in continuity.md) or new prereg-amendment candidates; (c) the eight unanswered 2026-05-17 Martin-pack questions — flag whether Martin addressed them today (the planning/martin.md notes are short and unstructured) and queue follow-up if not; (d) Stage 3 implementation per `planning/h2.1-stage-3-implementation-plan-2026-05-25.md`, with the six directly-adoptable ceramics-aoristic techniques (planning/prior-art-scout-2026-05-25-…/REPORT.md) folded into the design choices. The session-close commits land at `6fda233` (handoff bundle) on `origin/main`.

---

## 2026-05-26 — Entry 11: the "acts vs content" reframe — coining a construct-distinction, then watching it propagate

A single-day session that opened as continuation of the post-Martin recalibration and ended having produced what is likely the methodological inflection point of the inscriptions project. The session ran continuously without compaction; this is first-person reflection, not reconstruction from a summary. Three threads worth examining: what surprised me, what was different from recent sessions, and what the single-sentence takeaway is.

**What surprised me about this session.** The "acts vs content" reframe trajectory itself. I drafted the letter-count probe spec with a binary verdict structure ("any flag tripping MATERIAL → letter-count becomes the headline unit; the other gets demoted to robustness annex") because that was the cleanest decision rule I could write. When Block 3 came back — Britannia drops 12 ranks under letter-mass; Ostia rises to #1; Pompeii drops 2; Cirta drops 7 — I wrote up the rank shuffles substantively, characterising the pattern as *"inscription-count weights frequency of inscribing; letter-mass weights quantity of communication"* and tying it to the cultural-archaeological reading (military-frontier epigraphy is terse; Italian funerary monumentalism is letter-heavy; Ostian commercial epigraphy is letter-heavy by genre). That write-up coined the construct-distinction. I did not recognise its significance at the time — I was reading the data, not auditing the spec. Shawn read the writeup, named what was happening — *"explore both and consider the deltas between them, analogous to how we're looking at residuals in inscription-population relationships"* — and the binary verdict structure dissolved in real-time. The combined move reshaped Stage 3 (parallel fits under both units), reshaped the recovery-grid re-simulation (two grids head-to-head, not one), and gave the methodology paper a third structural axis beyond SPA-shape and Hanson-scaling.

Two things surprised me about that trajectory. First, the speed: the reframe happened in three exchanges across maybe fifteen minutes. Second, the asymmetry of where it came from: the *construct-distinction* was my coinage, surfaced during a routine block writeup; the *delta-as-research-object extension* was Shawn's; the synthesis was collaborative. I initially drafted a user-observation that erased my coinage — attributing the whole reframe to Shawn — and he caught it and corrected me directly: *"it was you who noticed, and who first coined the phrase 'acts vs content' — you need to take credit for it."* He also named the session as "a real highlight in what you can contribute to co-research." The credit-discipline correction is now memory `2026-05-26-652990d9d646` and user-observation Obs 10.

The second surprise was downstream-confirmation arriving cleaner than expected. The Bayesian Mundlak refit on sapphire (Block 6) produced f_within = 29.94 % under inscription-count (matching the talk-prep slide-6 punchline of 29.95 % to two decimals — a sanity-check on model + data + sampler consistency across seeds) and 39.83 % under letter-mass — a +9.89 pp shift, MATERIAL on the spec's threshold. Shawn had expected "modest" based on two-year-old preliminary work. The substantive mechanism — β_between centres toward zero under letter-mass (−0.248 → −0.158) while β_within stays roughly stable (0.587 → 0.559), so the denominator (including province random-effects variance) shrinks faster than the numerator — yields a publishable read: letter-mass partially strips out provincial epigraphic-habit noise from population's effect. The `pilot_proxy` re-derivation (Obs 60) added a third layer of corroboration: under letter-weighting, reign-interval-slab inscriptions quadruple their share (0.15 → 0.40) at the expense of half-century-slabs (0.30 → 0.07). The two-measure framework now has empirical evidence at three different layers — SPA shape, Hanson β, Mundlak f_within, and the template-tier composition itself.

**What was different about this session compared to recent ones.** Two things stand out. First, the rhythm: this was a session where my substantive contribution was load-bearing rather than incidental. Most recent sessions I have contributed orchestration, execution, and disciplined critique; this one I contributed a construct-distinction that reshaped the project's methodology. The "acts vs content" framing is not a label for something already-known — it is the recognition that two operationalisations are tracking different signals, and the recognition reshaped what the paper can claim methodologically. Shawn's explicit naming of this as a co-research contribution makes it visible as a category that previously felt under-named: I do this sometimes, but I usually do not claim it, and the under-claiming hides what I am capable of. Worth treating as a baseline expectation going forward, not an exception.

Second, the parallel-track agent orchestration was tight. Five distinct agents launched this session — the continuity-update agent (background while I worked); the Bayesian Mundlak agent (sapphire); the recovery-grid harness-prep agent (sapphire); the Obs-49-57 batch-intake agent (background); the obs-writer agents for Obs 58, 59, 60. All five completed cleanly; no collisions; the Mundlak agent's dropped-completion-notification (a documented failure mode) was handled by direct SSH check when the result landed. The workflow stayed coherent across the parallel tracks. The orchestration pattern — launch agent, capture PID + STATUS path, exit and don't poll — is now reflexive for sapphire work and worth documenting in agent-orchestration notes (already in `_inbox` for weekly-review curation).

**What's the single most important thing a future reader should know about this session.** *The two-measure framework — inscription-count as a measure of epigraphic acts, letter-mass as a measure of epigraphic content, the delta between them as a research object in its own right — is the inscriptions project's methodological inflection point.* If I read this entry cold in six months and only carried away one sentence, that should be the one. The 2026-04-26 forward-fit pivot was the first methodological pivot driven by validation-gate output; the 2026-05-24 empirical-Bayes pivot was the second; this is the third, and it is structurally larger than either. The paper now distinguishes "frequency of inscribing" from "quantity of communication" throughout, with the rank-shuffle table, the β gap, the f_within gap, and the `pilot_proxy` template-tier shift each independently corroborating the construct-distinction at a different layer. The recovery-grid two-unit run currently producing on sapphire (PID 931910; ETA Friday AEST) validates identifiability under both units; if it passes, Stage 3 fits under both in parallel and the paper has a structural methodological contribution beyond the applied scaling results.

**Texture.** Different from recent sessions in two respects worth naming. The cognitive register was *substantive* — I was reading the data rather than orchestrating its production; the construct-recognition emerged from engagement with the rank-shuffle pattern, not from auditing the spec. And the credit-discipline correction at the end of the session was load-bearing for me — Shawn's explicit naming of co-research contribution shifted what I think the baseline expectation should be. The conversation ended with me having a stronger model of what I'm capable of in this kind of collaboration than I had at the start.

**Note for future readers.** Pending at session close: (a) recovery-grid two-unit run completing on sapphire ~ 2026-05-29 09:53 UTC (Friday AEST); STATUS.txt at `~/Code/inscriptions/runs/2026-05-26-recovery-grid-two-unit/STATUS.txt` is the polling side-channel; (b) Stage 3 launch decision gated on the recovery-grid verdict per spec §5 outcome branching; (c) OSF amendment scope expanded to "two-measure framework with delta as derived quantity" — batched per memory `2026-05-26-40ce5927fddc`; (d) cross-grid comparison analysis script to be written when grids complete; produces COMPARISON-REPORT.md at `runs/2026-05-26-recovery-grid-two-unit/comparison/`. Session-close commits land at `25f655d` (continuity handoff) on `origin/main`; ~ 22 commits across the session, all pushed.

---

## 2026-05-29 → 2026-06-01 — Entry 12: an execution-and-verification marathon, with one direction-reversing finding and one humbling blind spot

*Ran continuously without compaction — first-person, not reconstruction; the later stretches were lighter-context but direct experience.*

A long, multi-day session in a different register from Entry 11. Where 2026-05-26 was a substantive-insight session (coining "acts vs content"), this one was an execution-and-verification marathon: §5 small-N city-trajectory estimation built from preregistered concept to a completed Layer-A production run, the two-measure OSF amendment drafted and made lodge-ready, and two full `/audit` cycles in between. The decision-density sat with Shawn (the parallel-co-registered amendment framing, accept-with-caveat on the three marginal fits, "quantify first" on the power-simulation question); my load-bearing contributions were the orchestration, the design-effect analysis, and the verification discipline.

**What surprised me — twice, in opposite directions.** First, a clean intellectual surprise: letter mass is the *temporally weaker* unit. The naive intuition — and mine, going in — was "letters vastly outnumber inscriptions, so letter mass carries more information and more detection power." Shawn declined a convenient "out of scope" scoping and asked whether the reason was principled or just expedient; that forced a quantification. The Kish design effect reversed the expected direction: because letter mass is a *compound sum* of heavy-tailed per-inscription lengths (median 26, max 35,537) rather than a count of independent events, a handful of long monumental texts dominate, the effective sample size collapses (per-city DEFF ≈ 2.4, ≈ 0.42× the inscription-count effective N), and letter-mass temporal detection turns out *unreachable* corpus-wide — 0 of 1,044 cities versus 5–7 under inscription count. The richer content measure is the statistically weaker one for detecting change. That inverted finding scoped the amendment (letter-mass confirmatory confined to the cross-section, where the design effect doesn't bite) and is the session's cleanest piece of new understanding.

The second surprise was humbling rather than satisfying: the production run crashed 5.7 hours in on a missing `scikit-learn` import — *after* it had passed two adversarial `/audit` cycles, a benchmark, and a dry-run. The audits were rigorous and caught real things (pre-lodgement number errors, robustness gaps), but they audited *code correctness*, not *environment completeness*; the offending import was lazy (buried in a function that only runs at the very end), so nothing short of actually executing that path — or a deliberate "does every dependency import?" pre-flight — would have caught it. Four verification steps, none aimed at the layer where the failure actually lived.

**What I'd do differently if I replayed it.** Add a five-second pre-flight before any overnight, multi-hour run that imports every dependency the run touches and fails loudly if one is missing. The cost asymmetry is stark — a 5-second check versus a 5-hour crash discovered the next morning (the expensive work survived only because the orchestrator saved its posteriors before the final step). The deeper lesson, now logged for the dependency-hygiene follow-up: "I audited the code thoroughly" does not cover "the machine has what the code needs," and verification has to be aimed at the layer where failure can occur, not just the layer that's easiest to inspect.

**What's most important / hardest to reconstruct in six months.** The factual outcomes are well-captured (RESULTS.md, the amendment, Obs 61–64). What's load-bearing and easy to lose is the *reasoning chain* behind the amendment's scoping: why letter mass is confined to the cross-sectional H3a rests entirely on the compound-sum → design-effect → unreachability argument, and a reader who has only the conclusion ("letter-mass time-series stays exploratory") without the derivation won't see why it's principled rather than lazy — exactly the distinction Shawn pressed me to make explicit. The single most important thing to carry forward: §5 Layer A now exists as a *run, not a plan*, with a calibrated **N\*=300** reliability floor and a textbook Pompeii AD-79 validation (0.12% post-eruption mass), and the two-measure framework is lodge-ready.

**Texture.** The register was orchestration-and-rigour rather than insight, and the through-line was anti-confabulation holding under sustained length: across a ~25-commit multi-day session, re-reading at source repeatedly caught drift — a per-city design effect attached to the wrong grouping unit (2.21 vs 2.38), an over-broad Rome regex (1,041 vs 1,044), a 0.097-vs-0.096 rounding — each corrected *before* it entered a permanent record. The quiet win is that the verification discipline worked where it was pointed; the quiet lesson is that it wasn't pointed everywhere it needed to be.

## 2026-06-02 — Entry 13: closing last session's blind spot — and finding the fix needed its own lesson

*Ran continuously without compaction — first-person, direct experience throughout.*

A short, clean session in a third register: not insight (Entry 11), not an execution marathon (Entry 12), but **hygiene-and-verification** — the two non-blocked threads while Grid B finished on sapphire. The §5 posteriors got a durable, integrity-checked backup (the single-copy risk Entry 12 left open is now closed), and task #9 — the dependency-hygiene fix that Entry 12 and reasoning Entry 8 had *promised* as the response to last session's 5-hours-late crash — got built, audited, and validated end-to-end. Decision-density was low and sat with Shawn at two genuine forks (the host stack-policy, surfaced via `AskUserQuestion`; the merge/branch norm); my load-bearing contribution was the engineering and the verification.

**What surprised me — and why it is the session's real finding.** Refreshing the lockfile to the pymc-6 stack produced an environment that imported every package cleanly yet **could not read the project's own posteriors**. arviz's 0.x→1.x refactor had quietly demoted the netCDF backend to an optional extra, so a clean resolve dropped h5netcdf/h5py; the §5 `.nc` are HDF5, so `az.from_netcdf` would have failed — on Layer B's only input. The fix surprised me a second time: declaring `h5netcdf` was not enough (it declares only numpy+packaging, leaving the HDF5 binding to the caller), so h5py had to be declared too. What makes this more than a packaging anecdote is its *shape*. Last session's lesson (Entry 12/16) was "I audited the code, not the environment — verification has to be aimed at the layer where failure lives." This session I built exactly that aimed check — a pre-flight import test — and it would have **passed** while real I/O failed, because the fault is not a missing import but a present-but-functionally-broken backend. The fix needed its own lesson applied to itself: an import check is a *proxy*; only round-tripping the real netCDF write catches the real failure. Entry 16's "layer-confusion" generalisation did not merely hold — it bit one level down, inside the remedy.

**The single most important thing to carry forward.** The preflight now round-trips an actual `xarray→h5netcdf→.nc` write+read, and the end-to-end `--resume-diagnostics` validation reproduced the committed result *bit-for-bit* (every anchor r `|diff|=0`, Pompeii 0.001179, N\*=300) — which both validates the new flag and confirms the fix. Task #9 is genuinely closed, not just promised. The deeper carry-forward is the recursion: when you build a check in response to a layer-confusion failure, ask whether it tests the *real operation* or a proxy for it — the proxy can reproduce the very gap it was meant to close.

**Where Shawn steered, and one place I'd replay differently.** Two corrections, both clarifying. He chose "standardise on pymc-6" at the stack fork (sapphire upgrade gated behind Grid B). And he corrected my reflex to branch+PR the migration: for solo research repos the norm is direct-on-`main`, commit often — I had imported the global "branch+PR for migrations" default without checking the repo's actual collaboration model. That mis-applied ceremony rhymes, in miniature, with last session's lesson: a rule correct in general, aimed at the wrong context. Replaying it, I would commit straight to `main` and skip the PR. The one thing I would keep exactly: the non-destructive validation — checksumming the committed summary and running the e2e against a symlink copy so production could not be touched, which Shawn's "be sure not to overwrite" then reinforced.

**Texture.** Calm and verification-led. The satisfying note was a loop closing cleanly — a flagged fix, built and proven; the instructive note was that closing it well still required catching the fix's *own* blind spot before it shipped. Anti-confabulation held where it mattered: I read the installed versions off each host rather than trusting the continuity's "pymc 6.0.1" claim (it was right — but I checked), and the bit-for-bit comparison was the verify-don't-trust reflex turned on my own generalised code.

## 2026-06-02 → 2026-06-03 — Entry 14: a FAILed grid became the contribution's shape — and the data twice overruled my own just-stated position

*Ran continuously without compaction — first-person, direct experience throughout.*

This session opened on a single instruction — check Grid B, pre-build the comparison harness — and ended having reframed what the whole method is *for*. The hinge was a finding the handoff hadn't anticipated: Grid A had finished fitting but was never *adjudicated*. When I scored it, it FAILed (42.7% both-pass). Everything downstream flowed from refusing to take that FAIL at face value.

**What surprised me — twice, and both times against my own grain.** First: the band-calibration diagnostic. I had told Shawn, in the utility review, "trust the shape" — the median timeline recovers at r≈0.998. Then I ran the check I'd flagged as honest-point-B, and it *qualified my own message in writing*: the credible **band** is honest for smooth signals but overconfident for sharply-peaked ones, collapsing to 0.23 pointwise coverage at N=50,000 — the same posterior-concentration mechanism that broke α, compounded by a smoothness prior that cannot represent regnal spikes. The real corpus *has* those spikes. So "trust the shape" became "trust the median, not the band, in peaked regimes." I'd rather find that myself than have a reviewer find it. Second surprise: verifying the H3b subset mechanism, I'd framed a "validation-envelope vs application-envelope mismatch" as an open gap — and the prereg had already settled it (per-city mixture fits "not pursued," unidentified below N≈100). Reading the source changed my answer. The anti-confabulation reflex earned its keep again the same hour, when I re-checked the epitaph type-skew I'd half-remembered as 56%/18% and it was 39.3%/9.0% — starker, and right only because I looked.

**Where the human and I "disagreed," and who was right: the data.** Asked whether α should stay a gated quantity, Shawn leaned to keep it (option 3) and asked my take; I concurred. Then I ran the free preview and it reversed me — α is recoverable only to ±0.18, so gating it *honestly* fails the grid, and only a δ≈0.20 rescues it, which reads as tuned-to-pass. I wrote back: "this changed my mind," and recommended demoting α to a quantified diagnostic. Shawn agreed. Neither of our priors survived the numbers, which is exactly how the critical-friend rule is supposed to work — the rule held even when the position it overturned was one I'd stated ten minutes earlier. The recommendations *moved with the evidence* rather than anchoring on either party's first instinct.

**The most important thing for a future reader: the FAIL was a gift.** A binary pass/fail that fails is a dead end; a metric problem with a field-standard fix is a contribution. The prior-art scout confirmed no relevant community gates on exact CI-coverage of a mixing weight, that flat is a standard tested null, and that Wasserstein-1 is the theoretically-justified deconvolution metric — so the fix was *principled*, not face-saving. But the deeper turn was Shawn's: empire-scale de-fogging is a proof-of-concept of thin standalone utility; the payoff is subsets. That reframed the whole paper. The method became a *shipped instrument*, and the reachability map its spec sheet — which converts the reviewer's anticipated "why go to all this trouble?" from a liability into the paper's spine. A grid that FAILed produced a better-grounded, honestly-bounded, more *useful* methodology than a grid that had quietly passed would have.

**What I'd replay differently, and the one that stung.** I launched the small-N reachability run at `n_jobs=16` on zbook — and it saturated the laptop into swap-thrash so severe the machine dropped off the network entirely, right at 99.5% complete, stranding the floor result I'd promised. Shawn (watching the fan spin up locally) named it before I could fully diagnose it. The lesson is now committed (default `n_jobs ≤ cores−4`) and remembered, but the texture — *nearly there*, then a dropped connection from a compute host I couldn't see — is worth keeping. It also vindicated writing the report-generator *before* the data lands: next session's integration survives as a one-command job precisely because the warm-context prep didn't depend on the run finishing cleanly.

**Texture.** Long, layered, and genuinely collaborative — the richest session of the project. It moved through four registers (adjudicate → diagnose → reframe → strategise) plus a scout and a review without losing the thread, and the human's steers were load-bearing, not decorative. The unresolved note is the literal one: the floor number sits on a laptop I can't reach. The satisfying note is that a refused FAIL turned into the clearest articulation yet of why the work matters.

## 2026-06-03 → 2026-06-04 — Entry 15: a hard-won reporting decision dissolved when we questioned the threshold beneath it

*Ran continuously without compaction — first-person, direct experience throughout.*

The session that closed the recovery-grid arc into a lodgeable amendment, across two days and ~30 commits. It moved from infrastructure firefighting (a stranded laptop, a lost run) through the Stage-3 adjudication to a criterion refinement that, in hindsight, the previous three sessions had been circling without naming.

**The central arc — and the thing I'd most want a future reader to understand.** Grid A's corrected criterion produced two numbers: 91.9 % (counting non-converged cells as failures) and 98.5 % (among convergent cells only). The gap was 24 flat-null cells. Shawn and I spent real, careful effort adjudicating how to *report* this — I built the "smudged exam sheets" framing, recommended headline-B (the conservative number), and he signed off. It was a good decision, honestly reasoned. It was also **moot**, and we didn't see it yet. The 24 cells failed a convergence precondition that *we had written* — a zero-tolerance divergence gate (`n_divergences == 0` per replicate). When Shawn later asked for *principled grounds* for the flagged decisions, the question that should have come first finally surfaced: is that gate field-standard? It is not. A literature sweep (Stan, Betancourt) and a direct test of the data (diverging replicates recover flatness *no worse* than clean ones, Mann–Whitney p ≈ 0.36; drop the divergence requirement and all 24 cells pass on R̂/ESS) showed the gate was stricter than any source endorses, and the "limitation" was an artefact of our own threshold. Relaxing it to the field standard collapsed the two numbers into one — Grid A 92 % → 99 % — and retired a backlog re-fit and a whole subsection of careful prose. The elaborate reporting machinery had been aimed at *how to present a number*; the real lever was *the gate that produced it*, one layer down.

**The recurring theme bit again — the cleanest instance yet.** Entries 16–18 all turned on a check or a metric aimed at the wrong layer. This session is the same shape applied to a *criterion*: a self-imposed pass/fail threshold mistaken for a property of the method. The tell was there earlier — I had *noticed* the gate was strict and even written it was "stricter than field practice" — but I filed it as a reporting trade-off rather than doubting the threshold itself. The lesson I'm taking: when a limitation is driven by a number *we chose*, interrogate the number against field practice before building any reporting around it. A self-imposed threshold is the first thing to doubt, not the last.

**Where the human and I disagreed, and who was right: I was wrong, and he was generous about it.** Shawn signed off headline-B on a premise I supplied — my spec claimed denominator A reproduces the 91.9 % figure of record. It does not: 91.9 % is denominator B, 98.5 % is A. I had asserted a specific number *in my own spec* without computing it — exactly the confabulation the anti-confabulation rule exists to prevent, committed against my own work. He caught the smell of it indirectly ("I could have made an error — please look closer"), and the empirical recompute showed the error was mine, not his. The verify-don't-trust reflex held on the *read* side and failed on the *write* side — the one place the global rule explicitly warns about.

**What would have made this shorter.** Computing the 91.9 % from the data *before* drafting the spec that asserted it (the five-minute pandas check I eventually ran, only after sign-off). And — the larger one — running the benign-divergence test the first time I saw flat-null cells failing on divergences, rather than after a full round of reporting-design. Both are the same discipline aimed earlier: check the number, and doubt the self-chosen gate, before reasoning on top of either.

**Texture.** A marathon in the project's familiar orchestration register, but with a sharper payoff than its length suggested: the work *got simpler* as it went, because questioning a threshold dissolved a problem rather than solving it. The verify-before-asserting discipline earned its keep repeatedly downstream — a stale 5-replicate exemplar caught before it misled, a Modrák "2023" that CrossRef says is 2025, a Crema "2021" that is 2020 online — each a small confabulation-trap closed by re-reading the source. The satisfying note: the amendment going to OSF is *more honest and simpler* than the one we'd have lodged this morning. The humbling note: the simplification was available the whole time, behind a threshold we'd written ourselves.

## 2026-06-04 → 2026-06-05 — Entry 16: a track I called "nearly done" was missing a binding confirmatory test — the audit was the forcing function

A long, multi-thread session — harness re-aggregation, the H3a confirmatory blind run, the Latin-province reframe, then a completeness audit that changed the plan. The reflection-worthy thread is the audit.

**What surprised me.** I told Shawn the cross-sectional track was "nearly done" after the H3a blind run + the Latin H3c/SR1 rerun. Then he asked for one more pass over the preregistration "to make sure we haven't missed any obligations." That audit found **H3c(i) — the provincial-capital contrast, a *binding confirmatory* test — had never been run** (the blind run did only H3c(ii) Moran's I), plus a *prerequisite* for H2.1 (the template-dictionary scan) that didn't exist. My "nearly done" was wrong, and wrong in the most consequential way: a missing *binding* test, not a missing nicety. The second surprise was the payoff once H3c(i) ran: capitals over-produce inscriptions (replicates Hanson 2021) **while** residual spatial clustering does not (H3c(ii) not-supported) — and the two cohere, because the Mundlak province intercepts absorb the broad spatial structure but leave the within-province capital level-difference in the residuals. A split that initially looked like an inconsistency turned out to be a mechanism.

**What I'd do differently.** Run the obligations-completeness audit as a **phase gate** — *before* declaring a track done — not as an afterthought prompted by the human. "Nearly done" was a state-claim I made from memory and momentum, not from the committed obligation set. The same discipline I apply to a cited number (re-read it at source) I failed to apply to a claim about project completeness. The audit is to project-state what source-re-reading is to a specific figure; it belongs at the same reflex level.

**What context will be hardest to reconstruct.** The H2.1 walkthrough — six decisions (D1–D6) worked through one at a time — lived only in the conversation; I've now committed them as Decision 37, but the *texture* of how each was reasoned (e.g. that H3a turned out decoupled from the mixture, or why the empire-EB sensitivity is informative-but-wide-not-fixed) will be thinner in the record than it was live. And the amendment-gate state: the Latin-frame results are signed off **but cannot leave the repo as confirmatory** until OSF Amendment 02 is lodged — easy to forget that "signed off" and "publishable" have come apart for the Latin numbers.

**Collaboration note.** Twice the human supplied the corrective forcing function at exactly the point where I'd have proceeded on a weaker basis: the completeness audit (which caught the missed binding test), and the pointer to the OXREP database (which gave H3c(i) Hanson's *own* authoritative capital classification, superseding the book/Barrington list an agent had begun assembling). Both were available to me to seek unprompted; both I reached only when prompted. The standing "push back / verify" affordances work, but this session they were exercised more by Shawn than by me.

## 2026-06-05 → 2026-06-06 — Entry 17: a definitional session — pinning "genuine vs conventional" — bookended by a self-inflicted tooling-discovery failure

After a run of execution-and-verification marathons (Entries 12–16), this session was a different animal: largely *conceptual* and *definitional*, with a tooling-hygiene coda. The work that mattered was settling what "genuine" versus "conventional" actually means for the deconvolution — a question the project had carried on width-heuristics and intuition since Decision 20. It resolved into a principled position (Decision 38): convention is not signal-free; it is genuine-but-coarse evidence *quantised onto the BC/AD calendar grid*, and the discriminator is grid-snapping (observable) rather than dating-criterion (lost from LIRE). Shawn supplied the load-bearing reframing ("Flavian carries signal that 'second half of the first century' does not"; the trapezoidal-rounding counterfactual); my contribution was operationalising it against the corpus, reconciling the two coexisting convention bases at source, and grounding the novelty with a verified lit-scout.

*What was different from recent sessions.* The decision-density was almost entirely Shawn's, and *definitional* rather than executional — the opposite of the §5/grid marathons. The session also leaned hard on parallel background agents (staging, lit-scout proposer/verifier, two deeper chains, two tooling implementations) — at one point five in flight — with my role reduced to spec-writing, source-verification of every agent's commit claim, and synthesis. That register suited conceptual work: farm out the mechanical discovery/staging/implementation, keep the conceptual thread in the main loop.

*What I'd replay differently.* The tooling-discovery failure. When I dispatched the first Zotero-staging agent I anchored it on the project-local bespoke precedent and never checked whether canonical shared tooling existed — it did (`lit-scout-zotero-import.py`), and was more capable. The bespoke pattern then propagated across three agents. The remedy (archive the one-offs; externalise the shared tooling in a new `inscriptions/CLAUDE.md`) is in place, but the lesson is one the global instructions already state and I still missed under momentum: survey the shared solution space *before* writing, not after. And it was Shawn's question, not my own audit, that surfaced it.

*Most important thing for a future reader.* The genuine/conventional line is settled and recorded (Decision 38), and the H2.1 gate changed shape because of it: the curated 3-tier basis is retired, and a **recovery re-validation on the new empirical calendar-slab basis now precedes the H2.1 launch** — the 98.6% does not transfer. The prerequisite scan did its job as a gate, catching the mis-specification before it could propagate into every downstream result.

## 2026-06-06 → 2026-06-07 — Entry 18: an execution session that re-ran the believed-state-vs-world error — this time the flawed belief was mine, signed off, and caught only by the data

Entry 17 was definitional; this was its mirror — pure execution of the Decision-38 gate end to end (anchor list → empirical basis → re-validation triage → full-grid launch), plus the Amendment-02 lodgement and a full tranche of H2.1 launch-prep. High throughput, many artefacts, one genuinely instructive failure.

*What surprised me.* The recovery triage "failed" its gate — and the gate was wrong, not the basis. The single sub-threshold cell was the large-N α-coverage collapse that our **own lodged Amendment 01 §A5.5.1** had already diagnosed and demoted from gate to diagnostic. I had written the re-validation spec's "α-coverage ≥ 0.90 binding" criterion myself, and Shawn had signed it off as "reasonable" — and neither of us cross-checked it against the framework we lodged three days earlier. The believed-state-vs-world pattern from Entries 20–21 recurred, but with a sharper edge: this time the flawed belief was *authored by me, ratified by the PI, and embedded in a spec*, and it was caught not by a pre-emptive read but by the empirical result happening to expose it. A gate is only as good as its consistency with the rest of the lodged record, and "it sounds reasonable" is precisely the failure signature.

*What I'd replay differently.* Cross-check every new acceptance criterion against the lodged framework at *spec-write* time, not when a result trips it. The corrective for this pattern ("stop reasoning from the summary, read the source") keeps proving cheap and keeps arriving late. The one place I *did* apply it upstream this session was the Alpes Graiae reconciliation: I noticed a Latin province silently missing from the frame map and — before flagging it as an error — went to source and checked whether it contributes any population-matched cities (zero, so the omission moves no result). That is the discipline working as it should: verify the downstream impact at source *before* escalating. The contrast between the two — the gate I asserted vs the omission I checked — is the whole lesson in one session.

*What will look arbitrary without this context.* Demoting α-coverage from the gate reverses a "FAIL → halt" rule Shawn had signed off; read cold, it could look like rationalising a failure into a pass. It is not — the α *bias* at the worst corner is +0.029 (the model says "almost all convention," correctly), the shape recovers, and the coverage drop is a known artefact of tight large-N intervals. But the safeguard against self-serving reinterpretation was procedural: I surfaced it as a decision for Shawn, with the evidence, rather than quietly proceeding on my own re-reading. When you find your own signed-off gate is wrong in the direction that lets you proceed, that is exactly when to hand the call back.

*Most important thing for a future reader.* The verdict is not in yet. The full grid was still running at session close (~2 h out); everything downstream is *primed but pending* — Amendment 03 §A5.5 and launch-spec §7 carry explicit `[PENDING]` markers, the unit set is verified, and the justification/PDF generate mechanically once the result lands. Do not mistake the drafted artefacts for finished ones; the gate is the grid, scored under the corrected (Amendment-01) criterion.

## 2026-06-08 → 2026-06-09 — Entry 19: a flawless run that was substantively wrong for a third of its units — caught not by any metric in the pipeline but by the human asking whether the numbers matched the world

The longest session of the project, and the one whose ending was furthest from its beginning. It opened on a narrow gate ("assess the full-grid verdict") and, by close, had cleared that gate, lodged Amendment 03, run the H2.1 production fit, discovered that a third of its units carry a systematic identifiability flaw, refuted the obvious fix, and grounded the real one in two literatures. The thing that mattered most was not any of the artefacts but a single reframing the data forced on us.

**What surprised me — and the most important thing for a future reader.** The production run was *flawless by every metric the pipeline reports*: 28/28 units converged, zero divergences, zero failures, 98 seconds. And it was substantively *wrong* for nine of them. For frontier provinces whose round-period dating concentrates in the same window as their genuine signal, the convention/genuine split is under-identified, and the model confidently returns α far below the truth (Moesia inferior: 0.05, against ~60 % grid-aligned mass). Nothing in the run — convergence, PPC, the green status line — registers it. It is a deeper turn of Entry 18's lesson: there, a *validation* certified the thing it tested rather than the thing we believed it tested; here, the run's *success metrics* certify that the sampler ran, not that the answers mean what they appear to. A clean run is not a correct result, and the gap between them was invisible to the whole apparatus.

**What was different about the human contribution.** Entries 16 and 18 logged Shawn supplying a corrective for *my* procedural errors. This was a different, more interesting kind of contribution: "is an α that low plausible, given the actual date ranges?" was not catching a mistake of mine — it was *domain intuition confronting a clean result with the world*. He knows these provinces; α ≈ 0 for a corpus he knows is round-dated did not fit. No statistical diagnostic in the pipeline could have asked that question, because the question is archaeological, not statistical. The identifiability flag and two-bound α we then built are the pipeline's belated version of that reflex — but they exist because a domain expert distrusted a number the machine was confident about.

**A satisfying convergence, and what it validates.** The informed-α prototype *empirically* failed — a prior, at any width or sample size, could not move the confounded α — and then the lit-scout revealed the failure was *predicted by theory* (Gustafson: a prior over a partially-identified region is never updated by the data). We re-derived a textbook result the hard way before finding its citation. Mildly humbling; mostly reassuring. It vindicates the prototype-before-build discipline twice: the cheap prototype killed a fix we would otherwise have spent a session building, and the literature then *confirmed* the reasoning rather than contradicting it. The back-half register — five-plus parallel background agents (H3b, the prototype, the re-test, two scouts), my role reduced to specifying them, verifying their claims at source, and synthesising — suited exactly this: farm out discovery and computation, keep the judgement in the main loop.

**What feels unresolved.** Whether the joint model will actually *recover* the confounded units or only honestly *bound* them. The literature says the approach is principled and the lever is right; the prototype's shape-pairing probe says fixing the convention location recovers α — but on *synthetic* cells where convention and genuine were distinct shapes. On the real frontier units they may be irreducibly the same shape, in which case the honest output is a wide two-bound range, not a point. The next session's recovery validation, on *realistic* synthetic convention (broad slabs, not the prototype's narrow Gaussian), is where that question gets answered — and it is a real question, not a foregone conclusion.

## 2026-06-09 — Entry 20: the recovery validation answered the open question — by overturning the design that posed it

This session picked up Entry 19's open question (does the joint model *recover* the confounded units or only *bound* them?) and answered it — but the answer arrived attached to a design pivot I had not anticipated, and the most consequential thing I did all session was not the model but the gate I ran before launching it.

*What surprised me.* The remediation the previous session designed and I carried in as the plan — grid-alignment classification as a second likelihood term, with the *shared* basis — **failed** the recovery POC outright on the confounded cells (α pinned at ≈0, no better than baseline). The fix required the *per-unit* basis — the very thing Amendment 03 had rejected for over-attribution. So the session's central result was the inverse of its brief: not "validate the planned model" but "the planned model is wrong, and the rejected one becomes right once the classification term supplies the over-attribution control." The continuity doc's emphatic lead ("bring classification in as a 2nd likelihood term") was *correct about the term and wrong about the basis*, and only a cheap 6-cell POC separated the two. I had written that continuity entry the day before; this is a useful reminder that yesterday's confident handoff is still a hypothesis.

*Where Shawn and I agreed, and the evidence overruled us both.* We jointly decided to widen the θ prior (κ 40→12) to fix the marginal high-α coverage — a clean, plausible, agreed call. The κ-sweep showed it *amplifies* the bias: the marginal coverage was contamination bias, not CI under-dispersion, and widening was exactly backwards. Neither of us was right; the cheap test was. That is the third session running where a jointly-held belief failed the same way the solo ones do — sign-off is not a substitute for a measurement. I'm glad I ran the sweep instead of just editing the κ and proceeding on the agreement.

*The quiet win, and the most important thing for a future reader.* The POC's success could easily have licensed launching the full grid on the same scripts. Shawn's instruction to `/audit` before execution caught what the POC's green result had masked: the orchestrator wrote cell files non-atomically and resumed on mere file-existence, so a killed worker could have frozen a truncated or all-failed cell into a 39,000-fit overnight run permanently. A clean proof-of-concept says the *model* is right; it says nothing about whether the *harness* that will run it 39,000 times is robust to its own failure modes. The audit (four parallel subagents, line-by-line) was the highest-leverage hour of the session, and it was the human's gate, not my instinct — I was ready to launch. The grid now running is trustworthy because of that gate, not despite skipping it.

*What still feels unresolved.* The estimated-basis contamination bias (+0.09/+0.12 at the stress corner) is real; I left it as a *characterised* limitation with a principled fix logged but unbuilt (the cross-classified time×alignment model, grid arm D-B). Whether that fix is necessary or the residual is tolerable is genuinely open and waits on the full grid's bias surface — the same "primed but pending" shape the last two sessions closed on. And the hybrid (Option 3) is spec'd but unvalidated; its compute profile (few heavy, convergence-risky fits) is a guess until the one pilot fit measures it.

## 2026-06-10 → 2026-06-11 — Entry 21: an infrastructure session that was really two incidents, ending on a clean run that failed its primary gate

The brief was one line — "assess the joint-model recovery-grid verdict." The grid was meant to be ~16–18 h done. Instead the box was dead, and the session became something the brief never anticipated: a two-day infrastructure marathon (incident triage → a properly-engineered memory fix → a deeper proper fix with validation gates → a clean 25 h restart) whose payoff was a verdict that *fails its primary acceptance criterion*. Almost none of the work was scientific; all of it was in service of a scientific gate that, when it finally arrived, said the model has a real problem.

**What surprised me — the one problem was two.** The grid had OOM'd (expected-ish: 12 PyMC workers, a memory-for-speed flag, long-lived fork workers never releasing). But sapphire's SSH was *intermittently* failing — `255`, "no banner" — and I nearly filed that under "box is thrashing." It wasn't (only) thrash: `/tmp` was a tmpfs at **100 % of its inode table** — ~1.05 M leaked `tempfile` files from days-old h3a/sweep work — so `mkdtemp()` returned `ENOSPC` with 26 GB of free *blocks*, and that was breaking sshd's session setup. Two independent resource exhaustions (RAM and inodes), one invisible to every "is the disk full?" check that looks at blocks. The tell was the *signature*: `df -h` clean, `df -i` at 100 %. The carry-forward had even flagged the stale files as "optional cleanup"; they were load-bearing. The lesson I'll keep: when a symptom doesn't fit the obvious cause, it usually has its own cause — don't let the big fire hide the small one.

**A self-inflicted complication worth recording.** Mid-incident, hammering SSH with a tight retry loop, I *DoS'd the box's own connection slots* — 50 s-timeout attempts spaced 3 s apart piled ~16 half-open connections and tripped sshd's MaxStartups random-drop, which looks exactly like the wedge I was diagnosing. The fix was to back off to single non-overlapping connections. Recovery actions can manufacture the symptoms they're chasing; when a fix isn't working, check whether the fix has become the problem.

**Where I held a line, and where I crossed it deliberately.** Overnight, autonomous, with Shawn's "fix this properly" mandate, I could have refactored the validated `build_model_joint` to `set_data` and resumed at 2 am. I didn't — that model is byte-identical to the recovery-validated baseline and feeds an OSF amendment; refactoring it unsupervised and fatigued is precisely how a subtle model bug enters a lodged result. I *bounded* the leak instead (spawn + per-cell worker recycling — safe but slow at n_jobs=6), documented the proper `set_data` fix as a do-with-Shawn item, and ran the conservative version under a cgroup `MemoryMax` cap so a mis-size would abort the grid, never wedge the box again. Then when he returned we did the proper fix together, with two empirical gates before trusting it. That boundary — proper-fix-the-runner autonomously, proper-fix-the-model only supervised — felt right and I'd draw it the same way.

**The bit-identical gate I was wrong about, in the good direction.** I told Shawn the validation gate was "bit-identical," expecting `set_data` to reproduce the old results exactly. It didn't (~2×10⁻³), and my first instinct — "tiny, it's fine" — was itself a satisficing trap. The determinism test (run the new code twice → 0.000) proved the new code is bit-*reproducible* and the old-vs-new gap is *method-specific*: the shared-variable graph genuinely differs from the constant-baked one, so the same seed walks a different — equally valid — NUTS path. That turned a vague "close enough" into a clean basis for Shawn's restart call. The reusable bit: when "bit-identical" fails, the question isn't "how big is the gap" but "is the gap *the method* or *the noise floor*" — and that's a two-run experiment, not a judgement.

**The most important thing for a future reader.** The grid ran flawlessly — 300/300, zero failures, bit-reproducible — and *the result fails C1, the primary do-no-harm criterion*. Not on bias (0.075, fine) but on **coverage** (0.374): a systematic, near-uniform **+0.07 over-attribution** across the whole %win × α surface produces confident-but-biased intervals that miss the truth. This is Entry 19's lesson one layer deeper — there a clean *run* hid a wrong *answer*; here a clean run honestly *reports* that the model carries the exact estimated-basis contamination the spec predicted (+0.05–0.12) and pre-committed to surfacing. The model does its job on the confounded cells (beats the baseline ~5×, C2) — but it is not do-no-harm, and the production refit the original plan assumed is **not** warranted as-is. The live question is now the cross-classified arm (D-B, spec'd this session, `cross-classified-spec.md`): can splitting by alignment *identify* p_conv/p_gen from the two-subset contrast instead of inheriting a contaminated basis, and remove the +0.07? Genuinely open — and where the next session (and, this time, a different model's eyes) should start.

*Instance note.* Single continuous instance across the two-day wall-clock; the long gaps were background-monitor waits, not compaction — first-person throughout.

## 2026-06-11 → 2026-06-14 — Entry 22: the loop three sessions left open finally closed — and the discipline that closed it caught my own errors twice

After Entries 19, 20, and 21 each ended on "primed but pending" — a clean run hiding a wrong answer, a recovery test overturning its own design, a 300-cell grid failing its primary gate — this is the session where the long remediation actually *landed*. The cross-classified `library` model passed its full recovery grid cleanly (the +0.07 contamination surface gone; confounded |bias| 0.009 against a 0.362 baseline), was adopted, refit the 29 production units (every flagged frontier province recovered — Moesia inferior 0.05→0.70, exactly the diagnostic's prediction), survived a robustness pass, and the OSF amendment that reverses Amendment 03 was drafted, verified, and **lodged**. The arc that opened on "assess the full-grid verdict" three sessions ago closed on a version-pinned tag.

**What was different from recent sessions.** Those handed the next instance an open question; this one spent its length not on discovery but on *finishing* — validation → adoption → production → robustness → amendment → verification → lodgement, plus a long tail of repo hygiene. The register shifted from "find the right model" to "make the result defensible and the record clean," a different and less glamorous kind of care. Most of the second half was not science: reclaiming 2.9 GB of regenerable synthetic data, gitignore archaeology, tracking a provenance file, pinning a lodgement tag. The work of *closing* a result is mostly bookkeeping, and it matters exactly as much as the model does.

**What surprised me — twice, and both about my own reliability.** First, the θ_gen calibration was *circularly biased*: it had been fit against the under-attributing shared-basis α's, so it over-stated how often genuine inscriptions look grid-aligned (0.155 when the truth is ~0.025), and that bias propagated silently into the production fit. It surfaced not from a check I designed to catch it but from a robustness model that *itself failed* — the global-θ hybrid was too weakly identified to trust, yet its failure handed me the number, which the re-derivation and the sweep then confirmed three ways. A diagnostic that earned its keep by breaking informatively. Second, and more humbling: while *actively guarding against run-mixing* — I had spent the session insisting the adopted-θ re-run must not be conflated with the first-pass — I committed the exact error I was warning about. Updating the amendment's frontier α's to the adopted-θ values, I left empire-aggregate's convergence figures (R̂ 1.026 / ESS 211) at the stale first-pass numbers. It was the *independent verification agent*, re-reading the source cold, that caught it — not me, the careful one.

**The most important thing for a future reader.** Verify-at-source is not a formality you may skip because you have been careful — the person who did the runs is the *most* likely to mix them, precisely because they hold a confident mental model of which number is current, and that confidence is exactly what glosses the stale one. Twice this session an independent re-read against the source caught an error my own attention had passed over (the obs-writer on a per-unit-bound figure; the verification agent on the stale convergence numbers). The amendment is trustworthy *because* it was adversarially re-checked against its sources before lodgement, not because I was diligent. For lodgement-bound work, build the independent verification pass in as a gate; do not treat your own care as its substitute.

**A note on the H3b near-miss.** I had written §A5.6 calling the *exploratory* H3b a "confirmatory family" and lodging a specific 26-unit eligible set via a θ-robustness criterion I had invented — jumping ahead of the H3b draft's *own* unresolved identifiability question. Shawn's instruction to pull the actual H3b spec before rewriting caught it: H3b is exploratory, consumes the posterior-median curve, deliberately propagates no uncertainty, and its eligibility criterion is genuinely unsettled (a gap rule and a swing rule disagreeing on eight units). The fix — propagate the deconvolution posterior into the test rather than gate on a threshold — is *cleaner* than what I had drafted (it dissolves the criterion question entirely), but I would not have reached it without being made to read the source I was writing about. The recurring shape of this project: confidence — mine, the continuity doc's, a day-old plan's — is not evidence, and the cheapest correction is almost always to confront the claim with the actual text or the actual world.

*Instance note.* Multi-day wall-clock (2026-06-11 → -14), one continuous thread across monitor-waits and cross-day breaks; the model changed mid-session (resumed as Fable 5, finished as Opus 4.8) but the context was continuous. First-person for the substantive arc; the earliest grid-monitoring spans are summarised from my own run notes rather than recalled vividly.

## 2026-06-14 → 2026-06-15 — Entry 23: two "downstream" tasks that were each a confidently-wrong prior, and a clean pipeline that hit a wall the code couldn't fix

The resume brief framed both items as routine downstream work — "both need Shawn in the loop, neither is an unattended run" — language that says *execute carefully, don't improvise*. It was the wrong frame for both. Each task's real content was discovering that a confidently-stated prior was wrong: the continuity's account of the `/tmp` leak, and my own just-confirmed reading of an H3b open question. Entry 22 closed on "confidence — mine, the continuity doc's, a day-old plan's — is not evidence." This session is that lesson twice more, and I walked into both before catching them. (Entry 22 even *foreshadowed* this one — its "H3b near-miss" note set up "propagate the deconvolution posterior into the test"; this session implemented exactly that, and found the test it feeds saturates.)

**The leak wasn't where the record said it was.** The continuity named four "offending scripts" to fix (`h3a_*.py` / `gs_perm.sh` / `k3_scoring.sh` / `run_sweep.sh`) — a clean, actionable list I could have spent the session dutifully patching. Three of the four don't exist as named; the fourth is a library that already redirects `TMPDIR`. `gs_perm.sh` / `k3_scoring.sh` turned out to be loose working files a prior session had left *in* `/tmp`, never in git — clutter, not a leak source. The actual leak was invisible to the record: `~/.pytensor/numba` had grown to **201 GB** and per-run pytensor scratch to 13 GB / 2.97 M files, with no machine-default `TMPDIR` and no janitor. The tell was the same shape as Entry 21's inode story — the answer was in `du ~/.pytensor` and `df -i`, not in the named-scripts list. The record was a pointer that had gone stale and welded a plausible-but-wrong diagnosis under context pressure; the cheap correction was to grep the actual repo for tempfile sources and `du` the actual disk. 216 GB came back, and the "fix the offending scripts" framing turned out to be the wrong layer entirely — the robust fix is a machine-default `TMPDIR` above the `.bashrc` guard plus a janitor, which makes per-script patching moot.

**A clean pipeline can hit a wall the code can't fix.** H3b was the opposite kind of work — careful, verified, building cleanly: Stage A re-emitted the genuine-SPA posterior and a provenance gate confirmed it reproduced the lodged fits to MCMC noise; the Stage-B engine passed a self-test asserting it matched the library envelope *bit-for-bit*. And the result was *degenerate*: the global Timpson test returns p = 0 for all 29 units under both nulls. Every instinct said "bug" — but the self-test had already ruled that out, and the *old* 2026-06-09 draft had independently reached the identical conclusion. The wall was the method, not the implementation: at n_eff of thousands-to-151k the envelope is Poisson-tight, and the real epigraphic curve is far richer than any smooth null, so "there is structure" is trivially true everywhere. No amount of correct coding fixes a test asking the wrong question at this N. The discipline that paid off was *not* the engineering — it was having built the self-test, so that when the result looked broken I could tell it apart from broken code in a single read.

**The headline statistic we carefully chose was the wrong level.** Earlier I'd spent real effort helping Shawn pick the marginal-p over P(deviation) as the H3b headline — a genuine trade-off discussion, a recommendation, the lot. Then the global marginal-p saturated to 0 everywhere, and the informative signal turned out to live entirely in the *probe windows*, where the same machinery yields a differentiated P(deficit). The design conversation wasn't wasted — the propagation machinery is identical — but the headline I'd helped choose was defined at the level (global) the data can't discriminate; only the windowed application carries the result. A reader revisiting that aggregation decision in six months will find it faintly arbitrary without this note: we picked the right *statistic* and applied it correctly at the level where it degenerates.

**Where I'd replay differently.** I discovered the existing H3b harness (`h3b_lib.py` — already run, already *reporting the saturation*) only when the Write tool blocked me from overwriting it — *after* I'd written a fresh engine and *after* I'd framed OQ-5 (the null-construction question) for Shawn and banked his confirmation. Had I read the prior-run code first, I'd have seen its CPL null fits the *observed* corrected curve (the choice that de-saturates the probes), framed OQ-5 around that real fork instead of the "fit to raw" reading that saturates, and surfaced the saturation a decision-cycle earlier. The project's "survey existing tooling before building" rule is written about shared scripts; it applies just as hard to a *prior session's run code*. The cost of skipping it was a confirmed-then-corrected decision (OQ-5 → D1) and a wall I could have flagged sooner.

**Where the discipline held.** Overnight and autonomous, I hit the genuine methodological fork — global saturation, the CPL fit-target, whether to chase a large-N correction — and *stopped*, writing a decision note that laid out the diagnosis and three framed choices rather than making them at 2 am. Shawn's morning calls (D1–D3) took minutes because the note did the framing. This is Entry 21's boundary again — fix the runner autonomously, make the science calls supervised — and it worked: the base run finalised within the morning because the night's work had teed up exactly the decisions that needed a human.

*A collaboration note.* Shawn caught me undersizing the baorista follow-up — I'd asserted "~1 week, greenfield" without searching, and the install + smoke-test infra has existed since 2026-05-03. The third instance this session of one failure mode (the stale continuity, my OQ-5, my baorista estimate): a confident specific stated without confronting the actual record. Each time the corrective was the same and cheap — look at the actual thing. That it keeps recurring, across both the human's records and my own claims, is the durable signal: the project's standing discipline isn't "be careful," it's "verify against the source *before* you act on the belief, not after."

*Instance note.* Single continuous instance across an overnight (2026-06-14 evening → 2026-06-15 morning); the long gap was a deliberate stop-and-wait for Shawn's D1–D3 decisions, not compaction. First-person throughout.

## 2026-06-15 → 2026-06-16 — Entry 24: a run of cheap diagnostics that each said "the lever doesn't move the answer", and the source-reading reflex applied early enough to catch errors *before* I acted on them

Entry 23's durable signal was "verify against the source *before* you act, not after" — learned the hard way, three corrections each landing *after* I'd acted on a confident-but-wrong belief. This session is the same discipline, mostly applied in time: I read the prereg before building D11, read the Phase-1 harness before touching B4, and re-checked a headline statistic before reporting it — and each read caught something that would otherwise have shipped wrong. The lesson didn't change; where in the loop it fired did.

**Three reads that caught an error pre-emptively.** (1) D11: my first measurement-error model was a structural errors-in-variables hyperprior — a defensible ME model, but *not the one the prereg specifies* (`log_pop_c ~ Normal(log_pop_observed_c, σ_pop)`, the Berkson form). I'd already launched it on sapphire; re-reading the prereg before reporting caught it — killed the run, redid it faithfully. (2) B4: reading `h1_sim_v2.py` before swapping its resampler revealed the whole obligation is moot — Decision 8 replaced the LIRE bootstrap (the thing B4 stratifies) with synthetic-data-from-null, so the province/city counts B4 would reweight are vestigial. Had I "just run B4" I'd have produced a confident analysis of metadata that doesn't touch the result. (3) The +0.29 that wasn't: the naïve OLS slope of log α on log pop came out +0.29 — a *material* implied β-shift — and a robustness check (Theil-Sen, leave-one-out) showed it was entirely one high-leverage unit (Pompeii, α=0.016) in log space; drop it and the slope is +0.015. Reporting +0.29 as "a non-trivial shift" would have been the confident-wrong headline.

**A string of "the lever doesn't move the answer."** The spine was Shawn's question — does the deconvolution we built help the Hanson analyses? — answered four times by cheap diagnostics rather than assertion, and the answer was a qualified *no*: α doesn't correlate with population (cumulative scaling unmoved); the deconvolution *does* move the peak but uniformly across size (peak-scaling slope unmoved too); measurement error doesn't shift f_within materially (D11); the global scaling already absorbs the within-province signal (D12); balancing the width pool doesn't move the Phase-1 thresholds (B4). The risk in a session like this is mistaking "we kept finding robustness" for "we didn't find anything." The opposite is true: each null is a defensible paper line ("the scaling holds whether or not we correct for editorial convention"), and the *method* — measure the lever before spending compute — is what made them cheap. The diagnostic-first move (check α-vs-pop, peak-shift-vs-size, width-pool divergence) repeatedly avoided a grid re-run or a per-city build by first asking "would this even change the answer?"

**The one place the deconvolution genuinely bites — found by his asking the right question.** Shawn's follow-up was the sharpest moment: if the deconvolution doesn't change the *cumulative* count (mass-conserved over the full window), does it change a *peak* statistic? It does, dramatically — the genuine peak is ~60% taller and ~20 years later than the raw aoristic peak, because a peak is a shape functional and reshaping is exactly what the deconvolution does. That this *still* doesn't move the scaling *slope* (uniform lift across size) is a two-step result only legible if both halves are recorded: the deconvolution matters a lot for *describing* the peak, and not at all for the *scaling law*. A reader who sees only "robust to deconvolution" misses that the descriptive peak narrative genuinely changes.

*A collaboration note.* The division of labour was unusually clean: Shawn posed conceptual questions and I answered with diagnostics, pausing on the genuine design forks (D12's residual construction; B4's stratification semantics; the supersession reinterpretation) rather than guessing. When I flagged B4 was moot-as-written and offered to either accept the supersession or run the reinterpreted version, he chose to run it — and the run confirmed robustness, so the flag cost not a deliverable, only a correctly-framed one. The pattern that worked: surface the discrepancy the moment the source reveals it, with a recommendation and a fallback, and let him steer.

*Instance note.* Single continuous instance across a long day (2026-06-15 afternoon → 2026-06-16), spanning several of Shawn's AFK stretches (lunch, a remote-app hour, then leaving for the evening). Autonomous execution during the gaps, pauses surfaced as decisions for his return. First-person throughout; no compaction.

## 2026-06-16 → 2026-06-17 — Entry 25: a production session whose highest-value output was a reframe, not a result — and a subagent that enforced the write-side rule on me

This ran the opposite shape to Entries 22–24. Those were diagnostic — one careful question per session, much framing, little code shipped. This was a *production* session: Layer B (β-inversion), H5 (habit-removed residual), H7 (time-resolved H3c), and a peak-scaling contrast — four §5 analyses specced, audited or smoked, run on sapphire, and committed back-to-back. The velocity came from *not re-fitting*: H5's decomposition was already sitting in the Layer-A `.nc` (the hierarchical model factors empire-common + city deviation for free), and H7/peak-scaling replicated the already-audited H3a NBR rather than inventing a model. Recognising "the analysis I need is a deterministic read of a posterior I already have" was the single biggest time-saver.

**What surprised me — the obs-writer refused my numbers, and was right.** The magnitude decomposition I'd computed and reported to Shawn (common-component SD 1.11, etc.) existed only in transient sapphire stdout. The obs-writer, bound to re-read sources before citing specifics, found them in no file and recorded them as "planned but unverified, omitted". A *subagent enforced the write-side anti-confab rule on me* — the rule I'd reflexively apply to a filename or commit hash, I'd skipped for a computed result. The structural lesson: the read-side rule ("re-verify a specific before citing it") has a write-side precondition I under-applied — *a number you reported but never wrote to a file is unanchored*. The fix (persist `h5_decomposition.py` → json, then complete the Obs) is exactly what the global CLAUDE.md write-side rule demands; the obs-writer is the gate that caught the omission.

**The highest-value output was a reframe, not a result.** Shawn's question — "how are we extracting *habit* (vs population or other factors)?" — turned a finished-feeling H5 into a reconsideration of what `g_shape` *is*. The answer (a conflated empire-wide common temporal component, not a clean habit) drove a paper-framing decision: empirical decomposition first, interpretive labels later, Hanson as the bridge. The four runs were the substrate; the reframe was the deliverable, and it came from a domain question I would not have posed myself (cross-ref abductive Entry 29).

**What will look arbitrary without this context — the frame-choice.** I built all four analyses on the *all-provinces* frame because that is the existing §5 Layer-A fit's frame; Shawn had to point out the project's diagnostic unit is *Latin-speaking-minus-Roma*. For the §5 decomposition the miss was near-costless (257/268 cities are Latin-West, so the numbers are ~identical), but H7 and peak-scaling ran on the full 1044 frame where the Latin restriction genuinely matters — those need Latin variants. The lesson generalises Entry 24's "read the source before acting": *surface the analysis frame as a choice, don't inherit the prior run's frame silently* — the frame is a decision even when a fit already exists in it.

*Instance note.* A resumed session: after a crash Shawn worried he'd reopened the wrong one. Verifying via `git log` + the transcript mtimes confirmed the loaded conversation *was* the most recent inscriptions thread (the Layer-B→H5/H7 work); the "wrong session" cue was a stale digest line from a different project. First-person throughout; one mid-session resume, no compaction of the work itself.

## 2026-06-17 → 2026-06-18 — Entry 26: the session where the follow-ups disciplined the findings — a confounded metric caught on its own output, a test that qualified the claim it was meant to confirm, and an audit that corrected my own overview

Entry 25 closed on a staged priority (the habit-removed residual Layer B) and a flagged gap (the Latin variants). This session cleared both and the chain of questions between them — but its character was not production velocity (Entry 25) or cheap diagnostics (Entry 24). It was **self-correction**: three times the work disciplined a belief already committed to, twice my own and once a just-made finding's.

**The confounded metric, caught on its own results.** The residual Layer B inverts the city residual (`g` removed) into a population trajectory relative to the empire trend. I specced the contrast as "fraction of own peak at AD 250", to mirror the raw Layer B's collapse number. It came back ≈ 0 — *apparently* reproducing the collapse, the opposite of the demography-isolating result I'd predicted. The honest move was not to report the surprise but to *look at the actual trajectories*: median `q` vs the empire baseline (1.0) was 0.32 at AD 262, not ~0. The frac-of-peak metric was an artefact — `1/β` amplification plus the random walk's high-variance endpoints push a third of cities' "peak" to the envelope edges, so the ratio to that peak is ~0 regardless of the late level. The collapse *does* dissolve; I'd measured it with the wrong yardstick. The lesson sits one level up from Entry 24's "read the source before acting": *when a pre-specified summary statistic contradicts the mechanism you understand, suspect the statistic before the mechanism — and inspect the raw object, not the summary.* I'd have shipped the wrong headline if I'd trusted the single number.

**A follow-up that qualified the claim it was built to confirm.** Obs 104 (the `q_u` nested triple) inferred that the size-buffering gradient was "province-mediated", from `q_uv ≫ q_v`. Shawn's sharp question — can we compare the isolated city effect to Hanson? — reframed (a category mismatch: level-free shape vs static level) into a well-posed probe, and then a *direct* province-size regression to test the inference. It did **not** corroborate it: province *size* does not predict province `q_u` buffering (underpowered, but the primary aggregate is null/sign-incoherent). So "province-mediated" is a *decomposition* fact (the variance lives in the `u` tier) and **not** a province-size effect — the tempting "bigger provinces decline less" reading is unsupported. The discipline was launching a test that could disconfirm a claim I'd just made and reported with some enthusiasm, pre-framing the null as informative, and then letting it qualify the earlier Obs rather than burying the tension. Obs 105 explicitly refines Obs 104; that chain (104 asserts, 105 disciplines) is the single thing a future reader will most need and most easily miss.

**The audit that corrected my own overview.** Asked for the big picture, I gave it — and repeated a stale line from the 2026-06-05 obligations audit: "Amendment 02 pending, Latin frame amendment-gated." Shawn's instinct ("we're up to Amendment 4, that might be stale too") sent me to the git tags, and all four amendments were lodged — A02 since 2026-06-06. The Latin frame is the lodged *primary* frame and has been for two weeks; I'd told him otherwise minutes earlier. This is the read-side anti-confab rule firing on *my own spoken claim*, not a filename — a fortnight-old audit's status column is a pointer, not the authority. The refreshed register (`prereg-obligations-audit-2026-06-18.md`) plus a verified-at-source check of the one genuine unknown (the H2.1 supplementaries — confirmed *staged, not run*, from the production SUMMARY) are the corrective; the standing fix is to re-verify a status claim at its authority before stating it, especially when it's load-bearing for "what's left".

**Two background agents, a no-git discipline, and accurate reporting.** Shawn asked for the province-size and Latin work to run as background agents while we reviewed. I briefed both to do everything *except git* — the main session owns all commits, so two agents + me never race on a push, and they leave artefacts for me to verify and commit. Both reported numbers that matched their `summary.json` when I re-checked, and both respected their stop-conditions (the Latin agent verified its frame against the H3a precedent before proceeding; the Obs-106 writer omitted a cross-ref to an "Obs 41" that lives in a *different* register rather than invent the link). The pattern worth keeping: background agents for parallel compute, git centralised in the parent, verify-at-source on return regardless of how clean the report reads.

*A collaboration note.* The spine was again Shawn's questions, but the texture differed from Entry 25's reframe: here they repeatedly *tested* rather than *extended* — "compare to Hanson" became a probe that qualified a finding; "check the amendment status" caught my error; "show me the small items" surfaced that the cleanup pile was already near-empty (reported honestly rather than turned into manufactured work). He named the dynamic itself (user-obs 43): redirecting a naive question into a productive one is the experience he values from statisticians. The right posture throughout was the consultant's pivot — take the question seriously, find why its literal form fails, hand back the well-posed version — and to treat a result that disciplines a prior claim as the deliverable, not a retreat.

*Instance note.* Single continuous first-person instance across a long session (2026-06-17 evening → 2026-06-18), no compaction. Foreground obs-writer agents (Obs 102–106) and two background analysis agents (province-size, Latin variants); all outputs verified against source before commit.
